{
  "meta": {
    "timeMinutes": 40,
    "difficulty": "core",
    "objectives": [
      "Explain how Q,K,V come from the same sequence",
      "Trace how every token attends to every other"
    ],
    "prereqs": [
      "attention-intro"
    ]
  },
  "slides": [
    {
      "kind": "section",
      "title": "Part 1 — Why self-attention",
      "body": "You derived attention with separate inputs. Self-attention is the special, powerful case that powers every transformer."
    },
    {
      "kind": "text",
      "title": "Q, K, V from the *same* sequence",
      "body": "In self-attention all three projections come from one input sequence $X$: $Q=XW^Q,\\,K=XW^K,\\,V=XW^V$. Every token simultaneously asks a question (its query), advertises itself (its key), and offers content (its value) — *to every other token in the same sequence, including itself*. One layer rewrites each token as a relevance-weighted blend of the whole sequence."
    },
    {
      "kind": "text",
      "title": "Full reach in one step",
      "body": "Recurrence connects token 1 to token 500 through 499 sequential updates (path length $O(n)$, signal decays). Self-attention connects *any* two positions in a **single** operation — path length $O(1)$, fully parallel. That constant path length is *why* transformers learn long-range structure that RNNs couldn't.",
      "viz": "attention-heatmap-explorer",
      "vizProps": {
        "presetIndex": 1
      }
    },
    {
      "kind": "text",
      "title": "Every row is a distribution over the sequence",
      "body": "The weight matrix is $n\\times n$; row $i$ is a softmax distribution over which tokens token $i$ reads from. \"bank\" beside \"river\" vs \"money\" ends up with different output vectors purely because its attention row points at different neighbors — contextualization, emergent from training."
    },
    {
      "kind": "section",
      "title": "Part 2 — Formalize it"
    },
    {
      "kind": "text",
      "title": "Same formula, shared source",
      "body": "$\\text{SelfAttn}(X)=\\text{softmax}\\!\\big(\\tfrac{(XW^Q)(XW^K)^{T}}{\\sqrt{d_k}}\\big)(XW^V)$. Structurally identical to general attention — the only change is that Q, K, V are three *different learned views of one sequence*. Derive the specialization:"
    },
    {
      "kind": "question",
      "question": {
        "id": "sa_derive",
        "kind": "guided_derivation",
        "question": "From attention to SELF-attention",
        "goal": "You already derived attention; specialize it to one sequence attending to itself.",
        "steps": [
          {
            "prompt": "In self-attention, where do Q, K, V come from?",
            "motivation": "All three are linear projections of the SAME input — every token queries the same set it belongs to.",
            "accepts": {
              "mode": "choice",
              "options": [
                "All three are linear projections of the same input sequence",
                "Q from input, K/V from separate memory",
                "K from the labels",
                "V is the positional encoding"
              ],
              "correctIndex": 0
            },
            "hints": [
              "'Self' = same sequence."
            ],
            "reveal": "Q=XWq, K=XWk, V=XWv from the same X — each token attends over the whole sequence (incl. itself)."
          },
          {
            "prompt": "Why is self-attention permutation-equivariant, and what fixes it?",
            "motivation": "It is a set operation with no built-in order; order must be injected explicitly.",
            "accepts": {
              "mode": "text",
              "rubricCriteria": [
                {
                  "id": "c1",
                  "description": "it treats the input as a set — no inherent order"
                },
                {
                  "id": "c2",
                  "description": "positional encodings inject order information"
                },
                {
                  "id": "c3",
                  "description": "so order must be added explicitly to the embeddings"
                }
              ]
            },
            "hints": [
              "Shuffle the tokens — does the math change?"
            ],
            "reveal": "No positional term ⇒ permuting inputs permutes outputs identically; positional encodings break that symmetry."
          }
        ],
        "finalResult": "Self-attention = Attention(XWq, XWk, XWv) — one sequence attending to itself"
      }
    },
    {
      "kind": "section",
      "title": "Part 3 — Worked example & the quadratic wall"
    },
    {
      "kind": "text",
      "title": "Cost: O(n²) in sequence length",
      "body": "$QK^{T}$ is $n\\times n$ — for $n=4096$ that's ~16M scores *per head per layer*. Attention compute and memory grow **quadratically** with context length. This single fact drives a whole research line: FlashAttention (IO-aware exact attention), sparse/sliding-window patterns, and linear-attention approximations all exist to fight this $O(n^2)$ wall."
    },
    {
      "kind": "question",
      "question": {
        "id": "sa_q3",
        "kind": "multiple_choice",
        "question": "FlashAttention makes long-context attention much cheaper in memory. How?",
        "options": [
          "It computes exact attention but never materializes the full n×n score matrix — it tiles the computation to stay in fast SRAM",
          "It approximates softmax with a linear kernel",
          "It drops attention to a local window",
          "It lowers everything to int8"
        ],
        "correctIndex": 0,
        "explanation": "FlashAttention is exact — same result as standard attention. The win is systems-level: it fuses the softmax and matmul and tiles them so the giant n×n matrix never leaves fast on-chip memory, cutting memory traffic dramatically. It does not change the math, unlike sliding-window or linear-attention approximations."
      }
    },
    {
      "kind": "section",
      "title": "Part 4 — Causal masking (the pitfall that bites)"
    },
    {
      "kind": "text",
      "title": "Don't let a token see its own answer",
      "body": "For autoregressive generation, token $i$ must predict token $i{+}1$ using only tokens $\\le i$. If training attention can see the future, the model learns to \"cheat\" — fluent under teacher forcing, garbage at inference where there is no future. The fix: set pre-softmax scores for $j>i$ to $-\\infty$ (a lower-triangular mask). This train/inference mismatch is one of the most common transformer bugs."
    },
    {
      "kind": "question",
      "question": {
        "id": "sa_q1",
        "kind": "multiple_choice",
        "question": "In causal self-attention, can the token at position 3 attend to the token at position 7?",
        "options": [
          "No — position 7 is in the future and is masked out",
          "Yes — every token attends to every other token",
          "Only if positions 4, 5, 6 are also attended to",
          "Only the first head can"
        ],
        "correctIndex": 0,
        "explanation": "Causal masking zeros attention from position i to any j > i. Position 3 may attend to 0,1,2,3 — never 4+. This is exactly what makes the model autoregressive: a prediction can only depend on the past."
      }
    },
    {
      "kind": "question",
      "question": {
        "id": "sa_q2",
        "kind": "drag_classify",
        "question": "Sort each transformer family by whether its self-attention uses a causal mask.",
        "items": [
          {
            "id": "gpt",
            "label": "GPT (decoder, text generation)",
            "bin": "masked"
          },
          {
            "id": "bert",
            "label": "BERT (encoder, classification)",
            "bin": "unmasked"
          },
          {
            "id": "llama",
            "label": "LLaMA (decoder LM)",
            "bin": "masked"
          },
          {
            "id": "enc",
            "label": "Encoder stack of a translation model",
            "bin": "unmasked"
          }
        ],
        "bins": [
          {
            "id": "masked",
            "label": "Causal (masked) self-attention"
          },
          {
            "id": "unmasked",
            "label": "Bidirectional (unmasked)"
          }
        ],
        "explanation": "Autoregressive decoders (GPT, LLaMA) must not see the future, so they mask. Encoders (BERT, the encoder side of a seq2seq model) classify/encode a complete input, so every token may see every other — no mask."
      }
    },
    {
      "kind": "question",
      "question": {
        "id": "sa_scen",
        "kind": "scenario",
        "question": "Diagnose the train/inference gap.",
        "scenario": "An autoregressive LM produces fluent text under teacher forcing in training but garbage when generating at inference. Attention is suspected.",
        "rubricCriteria": [
          {
            "id": "c1",
            "description": "the causal mask is missing/incorrect, so positions attend to future tokens in training"
          },
          {
            "id": "c2",
            "description": "the model learns to 'cheat' by peeking ahead — impossible at inference"
          },
          {
            "id": "c3",
            "description": "fix: apply the causal (lower-triangular) attention mask"
          }
        ],
        "explanation": "Without a causal mask, training lets each position see the answer (future tokens); at inference there is no future, so the learned shortcut collapses."
      }
    },
    {
      "kind": "question",
      "hints": [
        "Autoregressive = predict next token from past only.",
        "What must a position NOT be allowed to see?"
      ],
      "workedSolution": "It masks (−∞ before softmax) all positions j>i for query i, enforcing left-to-right information flow so training matches autoregressive inference.",
      "retryUntilCorrect": true,
      "question": {
        "id": "sa_mc",
        "kind": "multiple_choice",
        "question": "What does the causal mask in self-attention do?",
        "options": [
          "Zeros attention weights to future positions so a token cannot see what it must predict",
          "Normalizes the keys",
          "Injects positional information",
          "Reduces parameter count"
        ],
        "correctIndex": 0,
        "explanation": "The causal mask sets pre-softmax scores for future positions to −∞ so each token attends only to itself and earlier tokens."
      }
    },
    {
      "kind": "section",
      "title": "Part 5 — Connections & what's next"
    },
    {
      "kind": "explain_back",
      "question": {
        "id": "sa_teach",
        "kind": "explain_back",
        "prompt": "Explain self-attention to your pet vs. the attention it already learned.",
        "rubricCriteria": [
          {
            "id": "c1",
            "description": "Q, K, V all come from the same sequence"
          },
          {
            "id": "c2",
            "description": "every token attends to every token, including itself"
          },
          {
            "id": "c3",
            "description": "needs positional information added separately"
          }
        ]
      }
    },
    {
      "kind": "text",
      "title": "What to carry forward",
      "body": "Self-attention = attention with Q,K,V from one sequence: O(1) path length, O(n²) cost, and a mandatory causal mask for decoders. Next: **[multi-head attention](/paths/ml-engineer/lessons/multi-head-attention)** — why one attention isn't enough."
    }
  ]
}
