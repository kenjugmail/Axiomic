{
  "slides": [
    {
      "kind": "text",
      "title": "Following the slope",
      "body": "Imagine you're standing on a hilly landscape in thick fog. You can't see the bottom of the valley, but you can feel which direction is downhill under your feet. **Take a step in that direction. Repeat.** Eventually, you reach a low point.\n\nThat's gradient descent. The \"landscape\" is a loss function — a number that tells the model how wrong its predictions are. The \"position\" is the model's parameters (often billions of them). The \"steepest downhill direction\" is the gradient, $-\\nabla L$.\n\nThe contour plot on the right shows a 2D loss surface — a stretched bowl, $f(x, y) = x^2 + 5 y^2$. The trajectory shows gradient descent finding its way to the minimum at the origin, starting from the upper-right corner.",
      "viz": "gradient-descent-2d",
      "vizProps": {
        "learningRate": 0.05
      }
    },
    {
      "kind": "text",
      "title": "The size of each step matters",
      "body": "How big a step should we take? That's the **learning rate** $\\eta$. The update rule is:\n\n$$\\theta_{t+1} = \\theta_t - \\eta \\, \\nabla L(\\theta_t)$$\n\nToo small and you crawl. Too large and you overshoot — and on an *anisotropic* surface (one stretched in some directions, narrow in others) you can oscillate wildly along the narrow axis while barely moving along the wide one.\n\nLook at the trajectory now: with a much higher learning rate, the descent ricochets. The narrow axis (vertical) has high curvature; small movements there change the loss a lot. A learning rate that's well-tuned for the wide axis is too large for the narrow one.\n\nThis ill-conditioning is one reason **Adam** and friends exist — they automatically adapt the learning rate per parameter.",
      "viz": "gradient-descent-2d",
      "vizProps": {
        "learningRate": 0.18
      }
    },
    {
      "kind": "text",
      "title": "The update rule, explicitly",
      "body": "Stripped to its essentials, gradient descent is just one line of math:\n\n$$\\theta \\leftarrow \\theta - \\eta \\, \\nabla L(\\theta)$$\n\n- $\\theta$ is whatever parameters you want to optimize.\n- $\\nabla L$ is the gradient of the loss with respect to those parameters.\n- $\\eta$ is the learning rate — the step size scalar.\n- The minus sign is the only reason you go *down* the gradient instead of up.\n\nReal training pipelines wrap this with momentum, adaptive learning rates, weight decay, gradient clipping, mixed-precision arithmetic, distributed reductions, and a hundred other things. But every one of them sits on top of this one update."
    },
    {
      "kind": "question",
      "question": {
        "id": "lesson_q1",
        "kind": "slider",
        "question": "Drag the learning rate until gradient descent converges to the origin within 30 steps on this anisotropic surface.",
        "viz": "gradient-descent-2d",
        "min": 0.01,
        "max": 0.2,
        "step": 0.005,
        "default": 0.05,
        "target": {
          "min": 0.07,
          "max": 0.095
        },
        "explanation": "The optimal learning rate is bounded by 1/(largest curvature) — here that's 1/(2·5) = 0.1. Anything ≥ 0.1 oscillates along the y-axis; anything ≪ 0.1 crawls along x. The sweet spot is roughly 0.07-0.09."
      }
    },
    {
      "kind": "question",
      "question": {
        "id": "lesson_q2",
        "kind": "code",
        "question": "Implement one step of gradient descent. Given current params, gradients (same shape), and a learning rate, return the updated params.",
        "functionName": "gradient_descent_step",
        "starterCode": "def gradient_descent_step(params, grads, lr):\n    # All numpy arrays. Return updated params, same shape.\n    pass\n",
        "tests": [
          {
            "name": "Output shape matches input",
            "inputs": [
              "np.array([1.0, 2.0, 3.0])",
              "np.array([0.1, 0.2, 0.3])",
              "0.5"
            ],
            "check": "result.shape == (3,)"
          },
          {
            "name": "Zero gradient leaves params unchanged",
            "inputs": [
              "np.array([1.0, 2.0, 3.0])",
              "np.zeros(3)",
              "0.1"
            ],
            "check": "bool(np.allclose(result, np.array([1.0, 2.0, 3.0])))"
          },
          {
            "name": "Numerical fixture",
            "inputs": [
              "np.array([1.0, 2.0])",
              "np.array([0.5, -0.25])",
              "0.1"
            ],
            "check": "bool(np.allclose(result, np.array([0.95, 2.025])))"
          },
          {
            "name": "Works on 2-D arrays",
            "inputs": [
              "np.ones((2, 3))",
              "np.ones((2, 3)) * 2.0",
              "0.1"
            ],
            "check": "bool(np.allclose(result, np.ones((2, 3)) * 0.8))"
          }
        ],
        "explanation": "The minus sign is what makes it descent rather than ascent: params - lr * grads. Numpy broadcasting handles arbitrary shapes for free."
      }
    },
    {
      "kind": "text",
      "title": "Where it goes from here",
      "body": "Vanilla gradient descent is the bedrock, but in practice almost nobody runs it as-written:\n\n- **Stochastic gradient descent (SGD)** computes the gradient on a mini-batch instead of the full dataset — vastly cheaper per step, with noise that often helps generalization.\n- **Momentum** adds a fraction of the previous step to the current one, smoothing through narrow valleys.\n- **Adam / AdamW** maintain per-parameter adaptive learning rates plus momentum — robust to ill-conditioning, the modern default.\n- **Learning rate schedules** (warmup, cosine decay, linear decay) are essential for stable training of large models.\n\nEvery one of these inherits this lesson's basic geometry: you're following a gradient down a non-convex surface. The differences are in *how* you take each step. See [optimization theory](/wiki/optimization-theory) for the broader landscape."
    },
    {
      "kind": "text",
      "title": "Convexity, in one paragraph",
      "body": "A function $f$ is convex if $f(\\lambda x + (1-\\lambda)y) \\leq \\lambda f(x) + (1-\\lambda)f(y)$ for all $\\lambda \\in [0,1]$. Geometrically: line segments between any two points on the graph lie above (or on) the graph.\n\nProperties that make convex optimization easy:\n- **Any local minimum is global**.\n- **Gradient = 0** identifies the optimum.\n- **Convergence guarantees are sharp**: GD on smooth convex $f$ converges as $O(1/t)$; with strong convexity, $O(\\rho^t)$ for some $\\rho < 1$.\n\nNeural network training is **highly non-convex**. The loss landscape has many local minima, many saddle points (where most modern problems get stuck), and complex geometry. Why does SGD work anyway?\n\n- Most saddle points are easy to escape with random perturbations.\n- Most local minima have similar loss to the global minimum, especially in over-parameterized networks (loss landscape is mostly flat).\n- The 'effective optimization' isn't to the global min; it's to a region with good generalization.\n\nThe theoretical understanding has come far in the last decade (NTK, mean-field analyses, lottery-ticket hypothesis). But practical training relies heavily on heuristics that work empirically."
    },
    {
      "kind": "question",
      "question": {
        "id": "mc_opt_saddle",
        "kind": "multiple_choice",
        "question": "Saddle points in non-convex optimization are particularly problematic because:",
        "options": [
          "They are local minima you can't escape.",
          "Gradient is zero, so first-order methods can get stuck even though they aren't optima.",
          "They make convergence impossible.",
          "Adam ignores them entirely."
        ],
        "correctIndex": 1,
        "explanation": "At a saddle point the gradient is zero (it's a critical point), so vanilla gradient descent has no direction to move. Most saddle points are escaped by noise from mini-batch sampling (SGD) or by second-order methods. Adam's per-parameter adaptive step sizes also helps because tiny gradients along some directions get amplified by the running-variance normalization."
      },
      "hints": [
        "What does 'gradient is zero' mean for gradient descent's update?",
        "Why is a saddle different from a minimum?"
      ],
      "workedSolution": "At a saddle point, the gradient is zero — gradient descent's update is zero — so first-order methods make no progress. Unlike minima, saddles have *some* direction of decrease, but the gradient at exactly the saddle doesn't reveal it. Escape comes from SGD's noise, second-order methods, or adaptive methods like Adam whose effective step in low-curvature directions is larger.",
      "retryUntilCorrect": true
    },
    {
      "kind": "question",
      "question": {
        "id": "math_opt_conv",
        "kind": "math_expression",
        "question": "On smooth convex $f$ with optimum $f^*$, GD achieves $f(x_t) - f^* = O(1/t)$. If you want error $\\epsilon$, the number of iterations needed scales as:",
        "acceptedAnswers": [
          "1/epsilon",
          "O(1/epsilon)",
          "1/ε"
        ],
        "hint": "Solve $1/t \\leq \\epsilon$."
      }
    },
    {
      "kind": "question",
      "question": {
        "id": "scn_opt_landscape",
        "kind": "scenario",
        "question": "Why over-parameterized networks train well",
        "prompt": "Modern transformers have more parameters than training tokens. Classical optimization intuition would predict overfitting AND difficult landscape. In practice, gradient descent finds good solutions. Discuss one reason (NTK or lottery-ticket-style) in two sentences."
      }
    },
    {
      "kind": "explain_back",
      "question": {
        "id": "eb_opt",
        "kind": "explain_back",
        "prompt": "Explain in two sentences why Adam often 'just works' on non-convex problems while requiring less tuning than SGD with momentum.",
        "rubric": "Mentions Adam's per-parameter adaptive step-sizing handles bad conditioning across layers, removing the need for hand-tuned learning-rate schedules across different problem structures."
      }
    }
  ],
  "meta": {
    "timeMinutes": 25,
    "difficulty": "intro",
    "objectives": [
      "Distinguish convex from non-convex objectives.",
      "Reason about gradient descent's behavior on non-convex landscapes.",
      "Recognize when momentum / Adam improves on vanilla SGD."
    ],
    "prereqs": [
      "math-foundations"
    ]
  }
}
