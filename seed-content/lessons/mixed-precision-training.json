{
  "slides": [
    {
      "kind": "text",
      "title": "Why train in lower precision",
      "body": "Modern training runs in **mixed precision** by default — bf16 or fp16 for matmuls + activations, fp32 for optimizer state and a master copy of the weights. Three reasons:\n\n1. **Tensor cores demand it.** H100 hits ~989 TFLOPS at bf16, ~67 TFLOPS at fp32. Skipping the tensor cores costs 15× throughput.\n2. **Memory.** bf16 halves weight + gradient storage vs fp32. Bigger batches fit; bigger models fit.\n3. **Bandwidth.** Half the bytes on every HBM round-trip.\n\nThe catch: lower precision narrows dynamic range. Naive fp16 training underflows tiny gradients to zero, the model stops learning, and you don't notice until validation loss flatlines. Mixed precision is *engineered around* this; it works because of two specific choices.\n\nThis lesson covers the three options (fp16, bf16, fp8), what loss scaling does, and when each precision choice breaks."
    },
    {
      "kind": "text",
      "title": "Worked example: dynamic loss scaling",
      "body": "PyTorch's `GradScaler` starts with a large scale (~65536). After each backward pass, checks for `inf` / `NaN` gradients. If found: skip the optimizer step + halve the scale. If not: scale up after a few clean steps. The scale auto-tunes to the largest value that doesn't overflow — letting gradients use the maximum FP16 dynamic range. The whole machinery is invisible to model code; just wrap `optimizer.step()` in `scaler.step(optimizer)`. BF16 with its larger exponent range doesn't need any of this."
    },
    {
      "kind": "question",
      "question": {
        "id": "gd_mp_lossscale",
        "kind": "guided_derivation",
        "question": "Derive the appropriate loss scale",
        "goal": "FP16 min positive normal value ~$6 \\times 10^{-5}$. To represent gradients down to $10^{-9}$, what loss scale is needed?",
        "steps": [
          {
            "prompt": "Loss scale $S$ multiplies gradients by $S$ before FP16 conversion. We need scaled gradients $\\geq 6 \\times 10^{-5}$. With true gradient $g = 10^{-9}$: solve $S g \\geq 6 \\times 10^{-5}$.",
            "motivation": "Push gradients into representable range.",
            "accepts": {
              "mode": "math",
              "acceptedAnswers": [
                "6e4",
                "60000"
              ]
            },
            "hints": [
              "$S \\geq 6 \\times 10^{-5}/10^{-9} = 6 \\times 10^4$."
            ],
            "reveal": "$S = 6 \\times 10^4$ — close to the default starting value 65536."
          }
        ],
        "finalResult": "$$\\boxed{S \\geq \\text{min FP16}/\\text{min target gradient}}$$ Standard PyTorch GradScaler starts at 65536 and adapts. BF16 has the same exponent range as FP32 and needs no scaling."
      }
    },
    {
      "kind": "text",
      "title": "fp16, bf16, fp8 — the trade-offs",
      "body": "All three are 16-bit or smaller. The key difference is how the bits split between **range** (exponent) and **precision** (mantissa).\n\n| Format | Bits | Exponent | Mantissa | Range | Precision |\n|---|---|---|---|---|---|\n| fp32 | 32 | 8 | 23 | ~1e-38 to ~1e38 | high |\n| fp16 | 16 | 5 | 10 | ~6e-5 to ~6e4 | medium |\n| bf16 | 16 | 8 | 7 | ~1e-38 to ~3e38 | low |\n| fp8 (E4M3) | 8 | 4 | 3 | ~2e-3 to ~448 | very low |\n| fp8 (E5M2) | 8 | 5 | 2 | ~6e-5 to ~6e4 | very low |\n\n**bf16** keeps fp32's exponent. Same dynamic range, less mantissa precision. **It rarely needs loss scaling** because gradients don't underflow.\n\n**fp16** has narrower range. Small gradients (`< 6e-5`) flush to zero. Loss scaling fixes this by multiplying the loss before backward (so gradients land in the representable range), then unscaling before the optimizer step.\n\n**fp8** is even tighter. Two encodings: E4M3 for forward pass (more precision, less range), E5M2 for gradients (more range, less precision). H100 supports it natively; A100 doesn't.\n\n**Practical rule**: bf16 on Ampere+ for training. fp8 for hot paths on Hopper+. fp16 when you must (older hardware), with loss scaling."
    },
    {
      "kind": "text",
      "title": "Loss scaling, in detail",
      "body": "The standard fp16 recipe:\n\n```\nloss = forward_pass(...)\nscaled_loss = loss * scale\nscaled_loss.backward()           # gradients in fp16, scaled\noptimizer.step()                  # unscale + apply in fp32\n```\n\nThe `scale` (typically 65536) shifts the gradient magnitude up by 16 bits, so values that would underflow in raw fp16 land in the representable range. Before the optimizer step, gradients are unscaled (divided by `scale`) into fp32 buffers.\n\n**Dynamic loss scaling** (PyTorch's `GradScaler`):\n- Start with a high scale (e.g. 65536).\n- If any gradient overflows (Inf/NaN detected), skip the step and halve the scale.\n- After N steps with no overflow, double the scale.\n\nThis adapts to whatever the model needs. Early training may overflow; late training has tiny gradients that need a big scale.\n\nbf16 doesn't need this. The exponent matches fp32, so gradients don't underflow — they just lose mantissa precision. Most modern recipes pick bf16 for exactly this simplicity."
    },
    {
      "kind": "question",
      "question": {
        "id": "mp_q1",
        "kind": "multiple_choice",
        "question": "You're training a transformer on an A100 and gradients keep underflowing in fp16 even with loss scaling at 65536. What's the cleanest fix?",
        "options": [
          "Increase loss scale to 1 million",
          "Switch to bf16. A100 supports it natively, the dynamic range matches fp32, and loss scaling becomes unnecessary — gradients won't underflow. The trade-off is slightly less mantissa precision, which models tolerate well",
          "Train in fp32",
          "Use a smaller learning rate"
        ],
        "correctIndex": 1,
        "explanation": "bf16 is the standard answer for Ampere+ hardware. It removes the loss-scaling dance entirely and gives you the same compute throughput as fp16 on tensor cores. The reduced mantissa precision (7 bits vs fp16's 10) is a small cost — most networks are robust to it. fp16 made sense when bf16 wasn't supported; on modern hardware it's a legacy choice."
      }
    },
    {
      "kind": "text",
      "title": "Master weights and the standard recipe",
      "body": "Even when matmuls run in bf16, the **master weights** stay in fp32. Why?\n\nUpdates are tiny: `w += lr * grad`. With `lr = 1e-4` and `grad ~ 1e-4`, the update is `1e-8`. Add that to a bf16 weight at scale `~1` and you lose the update entirely — bf16 only has ~7 bits of precision.\n\nThe fix: keep an fp32 copy of every weight. Apply updates in fp32. Cast back to bf16 before the next forward pass. This is what `torch.cuda.amp.autocast` + `GradScaler` (or PyTorch's `bfloat16` recipe) does under the hood.\n\nMemory accounting per parameter, standard recipe:\n- bf16 weight: 2 bytes\n- fp32 master weight: 4 bytes\n- bf16 gradient: 2 bytes\n- Adam fp32 momentum + variance: 8 bytes\n- **Total: 16 bytes/parameter**\n\nFSDP (Sprint 55, model-parallelism lesson) shards this 16-bytes-per-param across N GPUs. With 8 GPUs you pay 2 bytes/parameter per GPU + a factor for the all-gather. That's how 7B and 70B models train on commodity clusters."
    },
    {
      "kind": "text",
      "title": "fp8 — the new frontier",
      "body": "H100 introduced native fp8 (E4M3 + E5M2). Compared to bf16:\n\n- **Throughput**: ~2× more TFLOPS. H100 hits ~1979 TFLOPS at fp8 vs ~989 at bf16.\n- **Memory**: cuts the bf16 weights/grads in half again (2 → 1 byte).\n- **Quality**: most networks tolerate fp8 *if* you use per-tensor or per-channel scaling (each tensor gets its own scale factor, computed on the fly, so the dynamic range is exploited per-tensor instead of per-format).\n\nWhen fp8 breaks: very small batches (insufficient samples to estimate the scale), unusual activation distributions (LayerNorm outputs, attention weights with extreme outliers), tiny embedding gradients (E4M3 underflows where bf16 would still resolve).\n\nThe practical recipe (e.g. NVIDIA Transformer Engine, FP8-LM):\n\n1. Forward in E4M3 with per-tensor scaling.\n2. Backward in E5M2 (more range, less precision — gradients have wider dynamic range).\n3. Master weights still fp32.\n4. Loss scaling becomes per-tensor scaling (more sophisticated than global fp16 scaling).\n\nResult: 1.5-2× throughput improvement on H100, with comparable final loss. Frontier-scale runs use it; smaller training jobs often don't bother because the engineering complexity isn't worth it below billions of parameters."
    },
    {
      "kind": "question",
      "question": {
        "id": "mp_q2",
        "kind": "multiple_choice",
        "question": "Why does fp8 use two different formats (E4M3 + E5M2) for forward vs backward?",
        "options": [
          "It's an arbitrary convention",
          "Forward activations need precision more than range (values are bounded post-norm); gradients need range more than precision (they span many orders of magnitude). E4M3 (more mantissa) for forward, E5M2 (more exponent) for backward, hits both constraints with 8 bits each",
          "Backward needs more bits than forward",
          "E5M2 is faster than E4M3"
        ],
        "correctIndex": 1,
        "explanation": "Forward activations after LayerNorm have a relatively bounded range — high precision gives crisp matmul outputs. Gradients during backward span many orders of magnitude — high range avoids over/underflow. Splitting the formats matches each pass to its actual numerical needs. This is why bf16 (high range, low precision) is often used end-to-end as a simpler fallback when fp8's per-tensor scaling is too much engineering."
      }
    },
    {
      "kind": "text",
      "title": "What's next",
      "body": "Mixed precision is foundational. Every other systems-engineer lesson assumes you've picked the right precision and configured the recipe correctly.\n\nNext: [[data-parallelism]] — synchronizing gradients across multiple GPUs.\n\nKey takeaways:\n- bf16 is the modern default for Ampere+; same range as fp32, no loss scaling needed\n- fp16 needs dynamic loss scaling to avoid gradient underflow\n- Master weights stay in fp32 — bf16 precision can't represent small updates\n- Memory budget per parameter: ~16 bytes for standard mixed-precision Adam\n- fp8 (Hopper+) doubles throughput again, with per-tensor scaling and E4M3/E5M2 split"
    },
    {
      "kind": "text",
      "title": "FP16 vs BF16, the one chart",
      "body": "**FP16** (IEEE half): 1 sign / 5 exponent / 10 mantissa. Range ≈ $10^{\\pm 5}$, precision ~3 decimal digits. **Underflows badly** in gradient tails.\n\n**BF16** (Brain Float): 1 sign / 8 exponent / 7 mantissa — **same dynamic range as FP32**, half its precision. No loss-scaling needed. Dominant format on TPUs and modern GPUs.\n\n**FP8 (e4m3 / e5m2)**: emerging. e5m2 is for gradients (range > precision); e4m3 for weights/activations (precision > range). Requires per-tensor scaling at runtime to stay in-range.\n\nIf the framework gives you a free choice: BF16 every time on hardware that supports it. The only reason FP16 + loss scaling persists is older accelerators that lack BF16 tensor cores."
    },
    {
      "kind": "question",
      "question": {
        "id": "mc_mp_loss_scale",
        "kind": "multiple_choice",
        "question": "Loss scaling multiplies the loss by ~$10^4$ before backprop and divides gradients by the same factor before the optimizer step. Why does this help under FP16 but not under BF16?",
        "options": [
          "BF16 has no exponent bits, so scaling does nothing.",
          "FP16's narrow exponent range underflows small gradient values to zero; scaling pushes them into a representable range. BF16's exponent matches FP32 and doesn't underflow on the same gradients.",
          "Loss scaling is a placebo and only works because it slows training.",
          "Both formats need it; BF16 just hides the scaling."
        ],
        "correctIndex": 1,
        "explanation": "FP16's minimum positive normal value is ~$6 \\times 10^{-5}$ — way above the magnitude of many small gradients, which become exact zero. Multiplying the loss by $10^4$ moves those gradients into FP16's range. BF16 has the same exponent range as FP32, so small gradients are already representable."
      },
      "hints": [
        "Think about which bits handle range (exponent) vs precision (mantissa).",
        "Underflow comes from running out of exponent range, not from running out of mantissa precision."
      ],
      "workedSolution": "Loss scaling targets *underflow* — gradients smaller than the minimum positive value the format can represent. FP16's 5-bit exponent means its minimum is around $6\\!\\times\\!10^{-5}$, which is bigger than many real gradient values, so they round to exact zero and learning stops. Scaling the loss by $10^4$ shifts those gradients into the representable range. BF16's exponent is 8 bits (same as FP32), so it already represents tiny gradients fine.",
      "retryUntilCorrect": true
    },
    {
      "kind": "question",
      "question": {
        "id": "scn_mp_nan",
        "kind": "scenario",
        "question": "NaN loss after FP16 transition",
        "prompt": "You switch a working FP32 trainer to FP16 mixed precision and the loss goes to NaN within the first 10 steps. What's the single most likely root cause and the single change that usually fixes it?"
      }
    },
    {
      "kind": "question",
      "question": {
        "id": "math_mp_memory",
        "kind": "math_expression",
        "question": "A model has 7B parameters. Stored as FP16, how many GB of GPU memory do the weights alone occupy? (1 GB = $10^9$ bytes)",
        "acceptedAnswers": [
          "14",
          "14 GB",
          "14.0"
        ],
        "hint": "FP16 is 2 bytes per parameter."
      }
    },
    {
      "kind": "explain_back",
      "question": {
        "id": "eb_mp",
        "kind": "explain_back",
        "prompt": "Explain in two sentences why BF16 has become the default for transformer training on modern accelerators, even though it has *less* numerical precision than FP16.",
        "rubric": "Mentions that range matters more than precision for gradient magnitudes; BF16 matches FP32 range and avoids the loss-scaling complexity entirely."
      }
    }
  ],
  "meta": {
    "timeMinutes": 25,
    "difficulty": "intro",
    "objectives": [
      "Distinguish FP16 vs BF16 by exponent vs mantissa allocation.",
      "Explain why loss scaling fixes FP16 underflow but isn't needed for BF16.",
      "Reason about when FP8 becomes safe (and when it doesn't)."
    ],
    "prereqs": [
      "gpu-architecture"
    ]
  }
}
