{
  "slides": [
    {
      "kind": "text",
      "title": "Arrays: the foundation",
      "body": "An **array** is a contiguous block of memory holding fixed-size elements. Indexing is $O(1)$: address = base + index × element_size. That's it. Every other data structure is built on top.\n\nThe two operations everyone needs:\n- **Random access by index**: $O(1)$.\n- **Linear scan**: $O(n)$.\n\nAppending to a fixed-size array is $O(1)$ until you run out of space — then $O(n)$ to copy into a larger array. **Dynamic arrays** (Python `list`, C++ `vector`, Java `ArrayList`) double the capacity on overflow, giving **$O(1)$ amortized append** — see [[memoization-vs-tabulation]] for amortized analysis in general.\n\nWhat arrays don't give you: cheap insertion in the middle (shifts $O(n)$ elements), cheap lookup by value (linear scan). For those: hash tables."
    },
    {
      "kind": "text",
      "title": "Hash tables: how O(1) lookup works",
      "body": "A **hash table** ([[hash-table]]) maps keys to values via a *hash function* + an array of buckets.\n\n```\nbucket_index = hash(key) % num_buckets\n```\n\nIdeal: each key lands in a unique bucket. Lookup is $O(1)$ — compute the hash, jump to the bucket, return the value.\n\nReality: two keys can hash to the same bucket. This is a **collision** ([[hash-collisions]]). Two strategies:\n\n- **Chaining**: each bucket holds a linked list of entries. Insert prepends; lookup scans. Average $O(1 + \\alpha)$ where $\\alpha = n/b$ is the load factor.\n- **Open addressing**: on collision, probe to the next bucket (linear, quadratic, or double-hashing). Faster cache behavior but degrades worse near full.\n\n**Load factor** $\\alpha$ is the master variable. Most implementations resize (doubling buckets) when $\\alpha > 0.75$, keeping expected chain length $\\leq 1$."
    },
    {
      "kind": "question",
      "question": {
        "id": "ah_q1",
        "kind": "multiple_choice",
        "question": "An attacker sends 10,000 inputs that all hash to the same bucket in your web server's request cache (no randomized hash). What's the lookup complexity per request?",
        "options": [
          "Still $O(1)$ — hash tables are always $O(1)$",
          "$O(\\log n)$ — Java HashMap converts long chains to BSTs",
          "$O(n)$ — every key in the same bucket means every lookup walks the full chain. This is the **hash flooding** attack: documented CVEs against Perl, Python, Ruby, Tomcat (2003-2012). Modern defenses use SipHash with a per-process random seed",
          "$O(n^2)$"
        ],
        "correctIndex": 2,
        "explanation": "Hash flooding turns a hash table into a linked list. Without a randomized hash, an attacker who knows the hash function can craft keys that all collide; insertion + lookup degenerate to $O(n)$. Real CVEs from 2003-2012 forced Python, Ruby, Java, Perl to switch to SipHash with per-process random seeds. Modern Java HashMap also converts long chains to BSTs to bound worst-case at $O(\\log n)$."
      }
    },
    {
      "kind": "text",
      "title": "Hash function quality matters",
      "body": "A hash function maps arbitrary keys to fixed-width integers. Three properties matter:\n\n1. **Uniform distribution**: hash outputs spread evenly across the integer range. Bad: `hash(s) = s.length`. Good: SipHash, MurmurHash, xxHash.\n\n2. **Determinism**: same input → same output. Without this, you can't find what you stored.\n\n3. **Speed**: hash is computed on every operation. SipHash is ~1ns per byte; SHA-256 is ~2ns per byte. For non-cryptographic uses, speed matters more than security.\n\n**When to choose what**:\n\n- **General-purpose, untrusted input** (web request paths, user-provided keys): SipHash with per-process random seed. Standard in Python `dict`, Rust `HashMap`, Ruby `Hash`.\n- **Internal, trusted input** (struct fields you control): MurmurHash3 or xxHash. ~3-4× faster than SipHash. Used in databases (RocksDB, Spark).\n- **Cryptographic integrity needed** (deduplication, content-addressed storage): BLAKE3 or SHA-2. Slower but collision-resistant against adversarial input.\n\nNever use `hashCode()`-style sums or XORs over fields. Distribution is poor; pathological inputs cluster easily."
    },
    {
      "kind": "text",
      "title": "Hash sets, hash maps, and sets vs ordered structures",
      "body": "A **hash set** is a hash table without values — just keys. Insertion + membership tests are $O(1)$ expected.\n\nA **hash map** is a hash table with key→value pairs. Same complexity, plus the value retrieval.\n\nWhen to choose hash vs ordered:\n\n| Need | Use |\n|---|---|\n| $O(1)$ lookup, no order | Hash table |\n| $O(\\log n)$ lookup, sorted iteration | BST or [[balanced-tree]] |\n| Range queries (\"keys between A and B\") | BST, B-tree |\n| Fastest insert + lookup, you don't need order | Hash |\n| Predictable worst case (real-time systems) | BST or [[trie]] |\n\n**Common ML uses of hashing**:\n- **Embedding lookup** for sparse features: dictionary of word → embedding row.\n- **Feature hashing** (\"hashing trick\"): map high-cardinality categorical features to a fixed-size vector via hashing. Avoids storing the full vocabulary; trades collisions for memory.\n- **Bloom filters**: hash-based probabilistic membership test using $k$ hash functions. Sublinear space; one-sided error.\n- **MinHash / LSH**: hashing for approximate nearest neighbors + set similarity."
    },
    {
      "kind": "question",
      "question": {
        "id": "ah_q2",
        "kind": "explain_back",
        "prompt": "Why does the Python `dict` use a randomized seed for hashing strings, even though it slows down every dict operation by a few nanoseconds? Frame your answer in terms of the trade-off it's making.",
        "rubricCriteria": [
          {
            "id": "attack-vector",
            "description": "Identifies hash flooding as the threat model: adversarial input crafted to cause collisions"
          },
          {
            "id": "real-world-impact",
            "description": "Connects to a real-world impact: web servers / parsers receiving untrusted dict keys can be DoS'd if hash is predictable"
          },
          {
            "id": "tradeoff",
            "description": "Names the trade-off: a few ns per op slowdown is acceptable to avoid worst-case $O(n)$ degradation under attack; the alternative (predictable hash) is a worse worst case"
          }
        ],
        "passingScore": 0.5
      }
    },
    {
      "kind": "text",
      "title": "Why O(1) is conditional",
      "body": "A hash table promises $O(1)$ average lookup. The worst case is $O(n)$ when every key collides into the same bucket — turning a hash table into a linked list.\n\nThree things keep average-case $O(1)$ achievable:\n\n1. **Good hash function** — uniform distribution over the bucket array. Bad hash $\\to$ pathological inputs.\n2. **Bounded load factor** — when filled > some threshold (~0.7 for open addressing), resize and rehash. Resizing is $O(n)$ but amortizes to $O(1)$ per insert.\n3. **Adversary resistance** — if attackers can choose keys, they can force collisions. Modern hashes use a random seed per process to mitigate.\n\nMissing any of these and your 'hash table' silently degrades to linear-time lookup at scale."
    },
    {
      "kind": "question",
      "question": {
        "id": "mc_hash_resize",
        "kind": "multiple_choice",
        "question": "A hash table with load factor 0.7 contains 700 keys and the bucket array has 1000 slots. You insert until load factor would exceed 0.75; the implementation doubles the bucket array size each resize. After the next resize, how many slots does the array have?",
        "options": [
          "1000",
          "1500",
          "2000",
          "2049"
        ],
        "correctIndex": 2,
        "explanation": "Doubling on resize: 1000 → 2000. After resize the new load factor is 750/2000 = 0.375, leaving plenty of headroom before the next resize. The growth pattern keeps amortized cost O(1) per insert."
      },
      "hints": [
        "Standard practice is to double the size on resize.",
        "Add one element, hit the threshold, double the array."
      ],
      "workedSolution": "Doubling resize: $1000 \\to 2000$ slots. Load factor immediately drops to $\\sim 0.375$. Doubling (rather than +N growth) is what makes the amortized cost of N inserts $O(N)$ instead of $O(N^2)$, via the standard amortized-analysis trick.",
      "retryUntilCorrect": true
    },
    {
      "kind": "question",
      "question": {
        "id": "math_hash_birthday",
        "kind": "math_expression",
        "question": "How many uniformly-random 32-bit hashes do you need before collision probability exceeds 50%? Use the birthday approximation $n \\approx \\sqrt{2 \\cdot 2^{32} \\cdot \\ln 2}$. Round to nearest 1000.",
        "acceptedAnswers": [
          "77000",
          "77163",
          "77000 hashes"
        ],
        "hint": "Birthday paradox: $n \\approx \\sqrt{2N \\ln 2}$ for $N = 2^{32}$."
      }
    },
    {
      "kind": "question",
      "question": {
        "id": "scn_hash_attack",
        "kind": "scenario",
        "question": "Algorithmic complexity attack",
        "prompt": "A web service uses a hash map keyed on URL path strings. An adversary submits 10,000 requests whose paths all hash to the same bucket. What happens, and what's the fix?"
      }
    },
    {
      "kind": "explain_back",
      "question": {
        "id": "eb_hash",
        "kind": "explain_back",
        "prompt": "Explain in two sentences why a hash table's *amortized* insert cost is $O(1)$ even though individual inserts that trigger a resize cost $O(n)$.",
        "rubric": "Mentions doubling growth, so per element the total resize work over its lifetime sums to O(n), giving O(1) amortized."
      }
    }
  ],
  "meta": {
    "timeMinutes": 20,
    "difficulty": "intro",
    "objectives": [
      "Explain the average-case O(1) and worst-case O(n) of hash-table operations.",
      "Compute load factor and reason about resize policy.",
      "Recognize when a hash collision attack is realistic."
    ],
    "prereqs": [
      "asymptotic-analysis"
    ]
  }
}
