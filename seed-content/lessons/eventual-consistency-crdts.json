{
  "meta": {
    "timeMinutes": 25,
    "difficulty": "advanced",
    "objectives": [
      "Distinguish strong from eventual consistency.",
      "Sketch a G-Counter CRDT.",
      "Recognize the use cases for CRDTs in practice."
    ],
    "prereqs": [
      "replication-strategies"
    ]
  },
  "slides": [
    {
      "kind": "text",
      "title": "Consistency models, the spectrum",
      "body": "Beyond CAP's 'C or A', consistency is a spectrum:\n\n- **Linearizable**: every operation appears to take effect at a single point in time between its start and end. Operations are totally-ordered, observable from any client.\n- **Sequential consistency**: operations are totally ordered, but order may not match wall-clock ordering across clients.\n- **Causal consistency**: causally-related operations preserve order; concurrent operations may reorder. Captures 'A wrote X, B read X, then wrote Y depending on X — every observer sees X before Y'.\n- **Read-your-writes**: a client sees its own writes immediately, but may see stale reads of others' writes.\n- **Eventual consistency**: all replicas eventually converge; intermediate states may be inconsistent.\n\nProduction systems mix-and-match. A social-media feed might be eventually consistent (your friend's new post takes seconds to propagate); your own profile might be read-your-writes (you immediately see your own edits); a payment transaction must be linearizable.\n\nThe weaker the consistency model, the cheaper the system. Picking the strongest model your use case requires (not stronger) is the design discipline."
    },
    {
      "kind": "text",
      "title": "Worked example: Yjs in collaborative editors",
      "body": "Yjs (Y-CRDT in JavaScript) powers Linear, Notion, Trello, and many collaborative editors. State-based CRDT for text + nested data structures. Each client maintains local replica, broadcasts ops, merges replicas. No central server required for correctness (servers help with persistence + connectivity). The CRDT machinery guarantees: any subset of clients can disconnect + reconnect; documents converge once everyone sees the same ops in any order."
    },
    {
      "kind": "question",
      "question": {
        "id": "gd_crdt_gcounter_conv",
        "kind": "guided_derivation",
        "question": "Prove G-Counter convergence",
        "goal": "G-counter state: vector of per-replica counts. Merge: elementwise max. Show idempotent + commutative + associative.",
        "steps": [
          {
            "prompt": "Idempotent: $\\text{merge}(s, s) = s$? (Elementwise max of $s$ with itself.)",
            "motivation": "Direct.",
            "accepts": {
              "mode": "choice",
              "options": [
                "Yes: $\\max(s_i, s_i) = s_i$ for each component.",
                "No.",
                "Only sometimes.",
                "Requires equal inputs."
              ],
              "correctIndex": 0
            },
            "hints": [
              "Max of equal values."
            ],
            "reveal": "Yes — elementwise max idempotent."
          },
          {
            "prompt": "Commutative + associative? (Both follow from max's properties.)",
            "motivation": "Standard.",
            "accepts": {
              "mode": "choice",
              "options": [
                "Yes — max is commutative + associative, so elementwise extension is too.",
                "No.",
                "Only commutative.",
                "Only associative."
              ],
              "correctIndex": 0
            },
            "hints": [
              "Componentwise extension of an associative + commutative operator inherits both."
            ],
            "reveal": "Yes. Merge is idempotent, commutative, associative → CRDT (state-based, CvRDT)."
          }
        ],
        "finalResult": "Convergence: idempotent + commutative + associative → all replicas converge to the same state given the same set of merges, regardless of order. The CRDT lattice property."
      }
    },
    {
      "kind": "text",
      "title": "CRDTs, conflict-free replicated data types",
      "body": "**CRDT**: a data structure designed to converge under concurrent updates without coordination. Two flavors:\n\n**State-based (CvRDTs)**: each replica's state is sent to others; merging is a commutative, associative, idempotent operation.\n\n**Operation-based (CmRDTs)**: each operation is sent to other replicas; operations must commute. Simpler when network is reliable.\n\n**Classic examples**:\n- **G-Counter** (grow-only): each replica increments its own counter; the value is the sum across replicas. Merge = elementwise max. Trivially convergent.\n- **PN-Counter**: pair of G-counters (increments + decrements). Supports decrement.\n- **OR-Set**: add + remove with unique tags. Adds and removes commute under tag-based reconciliation.\n- **LWW-Register**: last write wins by timestamp. Simple; loses data on concurrent writes.\n\n**Production use**: Riak's data types (counters, sets, maps, registers), Redis CRDTs, Yjs and Automerge for collaborative text editing (Figma, Linear, Notion's offline mode).\n\nThe value: build eventually-consistent systems that NEVER need application-level conflict resolution. The cost: limited to operations expressible in CRDT semantics. Arbitrary 'apply this function to two replicas' isn't a CRDT."
    },
    {
      "kind": "question",
      "question": {
        "id": "mc_crdt_gcounter",
        "kind": "multiple_choice",
        "question": "A G-Counter has two replicas. Replica A's local count: 5. Replica B's local count: 7. After they merge (elementwise max of the per-replica vectors {A: 5, B: 0} and {A: 0, B: 7}), the total counter value is:",
        "options": [
          "7",
          "5",
          "12",
          "Merge isn't possible without coordination."
        ],
        "correctIndex": 2,
        "explanation": "G-Counter state is per-replica: each replica counts its own increments. The total counter value is the sum across replicas. Vector ${A: 5, B: 0} \\sqcup {A: 0, B: 7} = {A: 5, B: 7}$ — elementwise max. Total = 5 + 7 = 12. Merge is commutative + associative + idempotent — exactly the CRDT requirements."
      },
      "hints": [
        "G-Counter stores per-replica counts; total is the sum.",
        "Each replica only increments its OWN slot; merge takes the max per slot."
      ],
      "workedSolution": "G-Counter: each replica $i$ stores its own count $c_i$; total = $\\sum_i c_i$. Merge: elementwise max of the count vectors. Replica A: ${A:5, B:0}$; Replica B: ${A:0, B:7}$. Merged: ${A:5, B:7}$. Total: 12. Correct because each replica only increments its own slot — no two replicas write to the same slot, so max equals 'take whichever has incremented more', which is the right thing.",
      "retryUntilCorrect": true
    },
    {
      "kind": "question",
      "question": {
        "id": "scn_crdt_docs",
        "kind": "scenario",
        "question": "Where CRDTs shine",
        "prompt": "A real-time collaborative text editor (think Google Docs) needs to handle simultaneous edits from multiple users with offline support. Why is a CRDT (or operational-transform) the dominant technical approach, and what's the tradeoff vs server-mediated coordination?"
      }
    },
    {
      "kind": "explain_back",
      "question": {
        "id": "eb_crdt",
        "kind": "explain_back",
        "prompt": "Explain in two or three sentences when CRDTs are the right tool and when they aren't.",
        "rubric": "Mentions that CRDTs are great for eventually-consistent collaboration on operations expressible as commutative merges; arbitrary operations (e.g., 'transfer money', 'reserve seat') don't fit and need stronger coordination; the right tool is matched to the operation's required consistency."
      }
    }
  ]
}
