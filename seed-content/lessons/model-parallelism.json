{
  "slides": [
    {
      "kind": "text",
      "title": "When DP doesn't fit the model",
      "body": "Data parallelism replicates the model on every GPU. That's fine until the model itself is too big for one GPU. A 70B-parameter model in bf16 with Adam: ~1120 GB of weights + grads + optimizer state. Eighty H100s, even? They each still need to hold the full state — DP doesn't share it.\n\n**Model parallelism** shards the model itself across GPUs. Three flavors, each attacking a different axis:\n\n1. **Tensor parallelism (TP)**: split each layer's weight matrix across GPUs. Every GPU does a slice of every matmul, with collective ops to glue partial results.\n2. **Pipeline parallelism (PP)**: split the layers themselves across GPUs. GPU 0 has layers 1-10, GPU 1 has 11-20, etc. Pass activations forward through the pipeline.\n3. **Fully Sharded Data Parallel (FSDP) / ZeRO**: the magic of the last few years. Shard weights, gradients, and optimizer state across GPUs while keeping the per-step compute pattern of vanilla DDP.\n\nReal frontier-scale runs combine all three: TP within a host, PP across pipeline stages, DP/FSDP across replicas. This lesson covers each + when to use which."
    },
    {
      "kind": "text",
      "title": "Worked example: Llama 3 70B training topology",
      "body": "Meta trained Llama 3 70B on ~16K H100 GPUs. Typical setup: tensor parallel within a node (8 GPUs/node over NVLink), pipeline parallel across nodes, FSDP for additional sharding. Tradeoffs: TP keeps communication local (fast NVLink); PP uses cross-node infinity-band (slower); FSDP overlaps with compute. The exact mix is tuned per cluster + model. Typical bubble fraction (PP): 10-20%."
    },
    {
      "kind": "question",
      "question": {
        "id": "gd_mp_bubble",
        "kind": "guided_derivation",
        "question": "Derive the pipeline-bubble fraction",
        "goal": "$S$ pipeline stages, $M$ microbatches. Total wall-clock = (M + S - 1) × stage time. Useful work = M × S × stage time / S = M × stage time. Bubble = ?",
        "steps": [
          {
            "prompt": "Bubble fraction = idle time / total time = $((M + S - 1) - M)/(M + S - 1) = ?$",
            "motivation": "Compute.",
            "accepts": {
              "mode": "math",
              "acceptedAnswers": [
                "(S-1)/(M+S-1)"
              ]
            },
            "hints": [
              "Subtract; simplify."
            ],
            "reveal": "Bubble fraction $= (S-1)/(M+S-1)$."
          },
          {
            "prompt": "For $S = 8, M = 32$: bubble = ?",
            "motivation": "Concrete.",
            "accepts": {
              "mode": "math",
              "acceptedAnswers": [
                "7/39",
                "0.18"
              ]
            },
            "hints": [
              "Plug in."
            ],
            "reveal": "$7/39 \\approx 0.18$ — 18% of pipeline time is bubble. Doubling $M$ to 64: $7/71 \\approx 10\\%$."
          }
        ],
        "finalResult": "$$\\boxed{\\text{Bubble fraction} = (S-1)/(M+S-1)}$$ Increase microbatch count to reduce bubble. Past some point, activation memory limits how many microbatches you can hold. Schedules like 1F1B (one forward, one backward) reduce activation memory further at the cost of bubble."
      }
    },
    {
      "kind": "text",
      "title": "Tensor parallelism (TP) — Megatron-style",
      "body": "Take a transformer's FFN: `out = (gelu(x @ W1) @ W2)`. With TP=4, split `W1` along its output dimension into 4 column shards, distribute one shard per GPU. Each GPU computes its column shard of `gelu(x @ W1)`. Then split `W2` along its input dimension correspondingly. Each GPU computes a partial sum of the output. An **all-reduce** across the 4 GPUs sums those partials.\n\nResult: every GPU does 1/4 the matmul work; the model fits in 1/4 the memory; one all-reduce per FFN block.\n\nFor multi-head attention, the natural split is **across heads**. With 32 heads and TP=4, each GPU owns 8 heads. The Q/K/V projections shard cleanly along the head dimension; the output projection's input dimension matches.\n\nWhen TP works:\n- High-bandwidth interconnect within a host (NVLink ~900 GB/s).\n- TP degree fits within a single node.\n- Layers are large enough that the matmul work matters more than the all-reduce overhead.\n\nWhen TP breaks:\n- Across hosts (inter-host bandwidth too low; the per-layer all-reduce dominates).\n- For tiny layers (overhead exceeds compute saved).\n- TP > 8 (most cluster topologies have only 8 GPUs per host on NVLink)."
    },
    {
      "kind": "text",
      "title": "Pipeline parallelism (PP)",
      "body": "TP splits *within* a layer. PP splits *across* layers. With 32 transformer blocks and 4 GPUs in a pipeline, GPU 0 holds blocks 1-8, GPU 1 holds 9-16, etc.\n\nNaively: GPU 0 forwards on a batch, sends activations to GPU 1, GPU 1 forwards, etc. Then backward in reverse. While GPU 0 is forwarding, GPUs 1-3 are idle. **Pipeline bubbles** waste compute.\n\n**Micro-batching** (GPipe / 1F1B / interleaved): split each batch into K micro-batches. As soon as GPU 0 finishes micro-batch 1, it starts micro-batch 2 — and GPU 1 is now busy on micro-batch 1. With enough micro-batches in flight, the bubble shrinks proportionally.\n\nPipeline bubble fraction is roughly `(P-1) / (M + P - 1)` where P is pipeline depth and M is micro-batch count. P=4, M=16 → bubble ~16%. P=4, M=4 → bubble ~43%.\n\nWhen PP works:\n- Across hosts. PP needs only point-to-point activation transfer between adjacent stages — much less bandwidth than all-reduce.\n- Models with many similar layers (transformers map cleanly to PP).\n- Combined with TP within stages: TP intra-host (NVLink), PP inter-host (slow interconnect).\n\nWhen PP breaks:\n- Tiny micro-batches → bubble dominates.\n- Variable layer cost (e.g., MoE) → load imbalance across pipeline stages."
    },
    {
      "kind": "question",
      "question": {
        "id": "mp_q1",
        "kind": "multiple_choice",
        "question": "You're training a 70B model on 32 H100s spread across 4 nodes (8 GPUs per node, NVLink within node, InfiniBand across). What's a sensible parallelism mix?",
        "options": [
          "DDP across all 32 GPUs",
          "TP across all 32 GPUs",
          "TP=8 within each node (NVLink-bound), PP=4 across nodes (low cross-node bandwidth tolerated by PP's lighter comm pattern). Optionally DP on top across replicas if memory allows. This matches each parallelism strategy to the right interconnect tier",
          "PP=32 across all GPUs"
        ],
        "correctIndex": 2,
        "explanation": "The classic '3D parallelism' recipe. TP wants high bandwidth (NVLink), so it lives within a host. PP wants point-to-point activation transfer (much cheaper than all-reduce), so it spans hosts. DDP/FSDP layers replicate or shard across the resulting groups. Megatron-LM's published recipes for 530B+ models use exactly this layout."
      }
    },
    {
      "kind": "text",
      "title": "FSDP / ZeRO — sharded data parallelism",
      "body": "FSDP (PyTorch) and ZeRO (DeepSpeed) attack the same problem from a different angle: keep the per-step compute pattern of DDP, but shard the *state* — weights, gradients, and optimizer state — across GPUs.\n\nThe ZeRO progression:\n- **ZeRO-1**: shard optimizer state across DP replicas. Saves ~50% of training memory (Adam state is 8 bytes/param vs 2 for weights).\n- **ZeRO-2**: shard optimizer state + gradients. Saves ~75%.\n- **ZeRO-3 / FSDP**: shard everything (weights, grads, optimizer). Saves ~90%+.\n\nFSDP's per-step pattern:\n1. **All-gather** the weights for the next layer (each GPU sends its shard, receives the rest).\n2. **Forward** through the layer using full weights (which now exist briefly on every GPU).\n3. **Free** the gathered weights — only the local shard remains.\n4. **Backward**: same pattern. All-gather, compute, free.\n5. **Reduce-scatter** gradients: every GPU ends up with the sum of its shard's gradients.\n6. **Optimizer step** locally on the shard.\n\nNet result: per GPU memory ~ `model_size / world_size`, plus a small buffer for the actively-gathered layer. Training a 70B model on 64 GPUs is ~1.75 GB per GPU for weights/grads/optimizer + ~few GB for the active layer + activations.\n\nBandwidth cost: roughly 2× DDP's all-reduce volume (the all-gather + reduce-scatter of weights, on top of the gradient reduce-scatter). PyTorch overlaps these with compute aggressively. Net throughput is competitive with DDP when you can't fit DDP at all."
    },
    {
      "kind": "text",
      "title": "Picking your strategy",
      "body": "A practical decision tree for choosing parallelism:\n\n```\nDoes the model fit on one GPU with DDP?\n├── Yes → use DDP. Done.\n└── No\n    ├── Use FSDP (default modern choice for single-host or close-coupled multi-host)\n    │   ├── Memory still tight? Add gradient checkpointing\n    │   └── Throughput too low? Increase per-GPU batch + use mixed precision\n    └── Frontier scale (>10B params, >32 GPUs)?\n        ├── Add TP within host (8-way TP standard)\n        ├── Add PP across hosts (4-8 stages)\n        └── DP across pipeline replicas\n```\n\nNot a religion. The right answer depends on hardware topology, model size, and target throughput. Megatron-LM's 530B run, GPT-NeoX's 20B run, and Llama's 70B run all use slightly different mixes. The principle: each parallelism degree has a bandwidth tax that should match the available interconnect at that degree's level.\n\nFor most teams, FSDP alone gets you to 70B. TP + PP becomes load-bearing past a few hundred billion parameters or for inference latency optimization (tensor parallelism on a single host is also the dominant *inference* parallelism — that's the next lesson)."
    },
    {
      "kind": "question",
      "question": {
        "id": "mp_q2",
        "kind": "multiple_choice",
        "question": "What does FSDP do that DDP doesn't?",
        "options": [
          "FSDP runs on more GPUs",
          "FSDP shards weights, gradients, and optimizer state across GPUs (only the locally-owned shard persists between layers); DDP keeps a full replica of all of these on every GPU. FSDP enables training models 8-16× larger than DDP fits, at the cost of additional all-gather traffic for weights",
          "FSDP doesn't use NCCL",
          "FSDP is only for fine-tuning"
        ],
        "correctIndex": 1,
        "explanation": "FSDP's per-GPU memory scales as `1/world_size` for weights+grads+optimizer, while DDP scales as O(1). The trade-off: FSDP all-gathers weights for each layer's forward + backward, doubling-ish the bandwidth volume. With well-tuned overlapping, FSDP throughput is competitive with DDP, and it's the difference between training a 70B model and not training it at all."
      }
    },
    {
      "kind": "text",
      "title": "What's next",
      "body": "Model parallelism is the foundation for any frontier-scale training. After this lesson the rest of the systems-engineer path is about putting it into production.\n\nNext: [[mlops-experiment-tracking]] — keeping experiments reproducible across many parallelism configurations.\n\nKey takeaways:\n- DP replicates; TP shards within layers; PP shards across layers; FSDP shards state\n- TP needs high bandwidth (NVLink within host)\n- PP tolerates low bandwidth (across hosts)\n- FSDP shards everything cheaply via all-gather + reduce-scatter\n- 3D parallelism: TP intra-host + PP inter-host + DP across replicas\n- For most teams, FSDP alone reaches 70B; TP+PP is frontier-scale only"
    },
    {
      "kind": "text",
      "title": "The three flavors of parallel",
      "body": "**Tensor parallel (TP)** shards individual matrix multiplications across GPUs (e.g., Megatron's column-row split). Each GPU sees the full batch but only computes part of each matmul; an all-reduce inside the forward+backward combines partials. Latency-sensitive; you typically run TP only within a node, over NVLink.\n\n**Pipeline parallel (PP)** assigns different *layers* to different GPUs. The data flows GPU 1 → GPU 2 → ... like an assembly line. Microbatches keep all stages busy; the bubble at start/end is wasted time. With $M$ microbatches and $S$ stages the bubble fraction is $(S-1)/(M+S-1)$ — that's the case for microbatch ≫ stage count.\n\n**FSDP / ZeRO** shards the optimizer state (ZeRO-1), gradients (ZeRO-2), and parameters (ZeRO-3) across DP replicas. The parameter shards are gathered on-demand for the layer that needs them. Trades extra communication for big memory savings — letting DDP-style training reach much larger models."
    },
    {
      "kind": "question",
      "question": {
        "id": "mc_mp_bubble",
        "kind": "multiple_choice",
        "question": "Pipeline-parallel training with 8 stages. You sweep the number of microbatches and see throughput improving up to ~32 microbatches, then flattening. Why?",
        "options": [
          "Above 32 microbatches the bubble fraction $(S-1)/(M+S-1)$ has dropped from ~22% at $M=32$ to under ~6% — additional microbatches give diminishing returns and just add scheduling overhead.",
          "The optimizer can't handle more than 32 microbatches.",
          "Past 32 you start running out of activation memory.",
          "Throughput is purely a function of stage count, not microbatch count."
        ],
        "correctIndex": 0,
        "explanation": "The bubble fraction is the source of non-perfect scaling. At $M=32, S=8$ it's $7/39 ≈ 18\\%$; at $M=64$ it drops to $7/71 ≈ 10\\%$. Each doubling of $M$ roughly halves the remaining bubble, so you see classic diminishing returns. There's a *separate* constraint from activation memory but that bites first as OOM, not as flat throughput."
      },
      "hints": [
        "The bubble fraction has $M$ in the denominator — it shrinks toward 0 as $M$ grows.",
        "Compute the bubble at $M=32$ and at $M=64$ and compare to the throughput curve."
      ],
      "workedSolution": "Bubble fraction is $(S-1)/(M+S-1)$. With $S=8$: at $M=32$ it's $7/39 ≈ 0.18$, at $M=64$ it's $7/71 ≈ 0.10$. Each doubling halves the remaining bubble, giving classic diminishing returns. Beyond a certain $M$ the bubble is negligible and the curve flattens at ~per-stage peak.",
      "retryUntilCorrect": true
    },
    {
      "kind": "question",
      "question": {
        "id": "scn_mp_topology",
        "kind": "scenario",
        "question": "TP across nodes",
        "prompt": "A teammate proposes running tensor parallel with TP=16 across two 8-GPU nodes connected by 200 Gb/s InfiniBand. Why is this a worse idea than TP=8 within a node, and what would you suggest instead?"
      }
    },
    {
      "kind": "question",
      "question": {
        "id": "math_mp_bubble",
        "kind": "math_expression",
        "question": "$S=4$ stages, $M=12$ microbatches. What is the bubble fraction (3 decimals)?",
        "acceptedAnswers": [
          "0.200",
          "3/15",
          "0.2",
          "1/5"
        ],
        "hint": "Bubble fraction = $(S-1)/(M+S-1)$."
      }
    },
    {
      "kind": "explain_back",
      "question": {
        "id": "eb_mp",
        "kind": "explain_back",
        "prompt": "Explain in two or three sentences when you'd pick FSDP over a TP+PP combination, even though TP+PP can also fit a giant model.",
        "rubric": "Mentions that FSDP is simpler to set up, scales well within a single replica group, and avoids the latency-sensitive intra-layer comms of TP; trade-off is more bandwidth used per step."
      }
    }
  ],
  "meta": {
    "timeMinutes": 30,
    "difficulty": "core",
    "objectives": [
      "Distinguish tensor parallel, pipeline parallel, and FSDP/ZeRO by what they shard.",
      "Recognize a 'bubble' in pipeline parallelism and reason about microbatch counts.",
      "Choose between TP + DP + PP combinations for a given model size and network."
    ],
    "prereqs": [
      "data-parallelism"
    ]
  }
}
