{
  "slides": [
    {
      "kind": "section",
      "title": "Part 1 — Meaning as geometry",
      "body": "Tokens are arbitrary integers — token 12 has no relationship to token 13. Embeddings fix that by giving every token a *location in space*, arranged so that **distance encodes meaning**. This space is the substrate every later layer manipulates."
    },
    {
      "kind": "text",
      "title": "Words become vectors",
      "body": "An **embedding** is a learned vector for each token. Picture a room where every word has a position: `dog` sits near `cat` (both pets), `king` near `queen` (royalty), `Paris` near `France` (geography).\n\nExplore the space on the right — notice that semantically related words cluster. A real transformer embedding has 768–4096 dimensions, but the geometric intuition (near = related) holds in any dimension.\n\nThe model is never *told* that dog and cat are similar. That structure **emerges** from training.",
      "viz": "embedding-explorer"
    },
    {
      "kind": "text",
      "title": "Similarity is direction (cosine), not length",
      "body": "\"How related are two words?\" usually means **cosine similarity** — the cosine of the angle between their vectors:\n\n$$\\cos(\\theta) = \\frac{u \\cdot v}{\\lVert u\\rVert\\,\\lVert v\\rVert}$$\n\n- Same direction → cosine ≈ 1 (very related).\n- Perpendicular → 0 (unrelated).\n- Opposite → -1 (anti-related).\n\nMagnitude is largely ignored for semantics (vectors are often L2-normalized). What matters is *which way* a vector points. This is the exact quantity attention will compute between queries and keys — so this geometry is not optional background, it is the mechanism."
    },
    {
      "kind": "text",
      "title": "Why this geometry appears: distributional training",
      "body": "Embeddings are **learned**, not designed. In a transformer the embedding layer is one matrix $E$ of shape $[\\text{vocab},\\, d_{model}]$; token id 42 simply fetches row 42. Gradients from next-token prediction flow back into $E$ and nudge rows.\n\nThe **distributional hypothesis**: words in similar contexts get similar vectors. `king` and `queen` co-occur with *royal, throne, crown*, so training pulls their rows together. Trained on hundreds of billions of tokens, the famous analogy structure $\\text{king} - \\text{man} + \\text{woman} \\approx \\text{queen}$ (Mikolov et al., 2013) emerges as a side effect."
    },
    {
      "kind": "section",
      "title": "Part 2 — Check your understanding"
    },
    {
      "kind": "question",
      "question": {
        "id": "emb_q1",
        "kind": "multiple_choice",
        "question": "Which pair would you expect to have the HIGHEST cosine similarity in a well-trained embedding space?",
        "options": [
          "doctor and nurse",
          "doctor and lamppost",
          "doctor and seventeen",
          "doctor and the"
        ],
        "correctIndex": 0,
        "explanation": "doctor and nurse share contexts (hospital, patient, medicine), so training drives their vectors into similar directions. The other pairs share little contextual structure, so their angles stay large (low cosine)."
      }
    },
    {
      "kind": "question",
      "question": {
        "id": "emb_q2",
        "kind": "multiple_choice",
        "question": "Two embedding vectors point the same direction but one is 10× longer. Their cosine similarity is:",
        "options": [
          "≈ 1 — cosine divides out magnitude, so only direction matters",
          "≈ 10 — longer vectors are more similar",
          "≈ 0.1 — the length ratio reduces similarity",
          "Undefined for vectors of different length"
        ],
        "correctIndex": 0,
        "explanation": "Cosine normalizes by both magnitudes (‖u‖‖v‖), so a pure scaling cancels out: same direction ⇒ cosine ≈ 1 regardless of length. This is exactly why semantic similarity is an angle, not a distance."
      }
    },
    {
      "kind": "question",
      "question": {
        "id": "emb_gd1",
        "kind": "guided_derivation",
        "question": "Derive cosine similarity and explain why dot product measures semantic closeness",
        "goal": "Show step-by-step why the dot product of two L2-normalized vectors equals their cosine similarity, and connect this to why attention (QK^T) measures semantic relevance.",
        "steps": [
          {
            "prompt": "Write the dot product u·v in terms of the magnitudes ||u||, ||v|| and the angle θ between the vectors.",
            "motivation": "The geometric definition of the dot product is the bridge between algebra and angle.",
            "accepts": {
              "mode": "math",
              "acceptedAnswers": [
                "u·v = ||u|| ||v|| cos(theta)",
                "u·v = |u||v|cos(θ)",
                "||u|| ||v|| cos(theta)"
              ]
            },
            "hints": [
              "This is the geometric definition of the dot product from linear algebra."
            ],
            "reveal": "u·v = ||u|| · ||v|| · cos(θ). This is the fundamental identity linking the algebraic operation (sum of products of components) to the geometric angle between vectors."
          },
          {
            "prompt": "If both u and v are L2-normalized (||u|| = ||v|| = 1), what does u·v simplify to?",
            "motivation": "Normalization is what makes dot product equal to cosine similarity.",
            "accepts": {
              "mode": "math",
              "acceptedAnswers": [
                "cos(theta)",
                "cos(θ)",
                "cosine of theta"
              ]
            },
            "hints": [
              "Substitute ||u|| = 1 and ||v|| = 1 into the equation from step 1."
            ],
            "reveal": "u·v = 1 · 1 · cos(θ) = cos(θ). For unit vectors, dot product IS cosine similarity. Many transformers work with L2-normalized key and query vectors for exactly this reason — the dot product directly scores angular closeness."
          },
          {
            "prompt": "In attention, the score for query q attending to key k is q·k (or q·k / √d_k). If embeddings of semantically similar words cluster in the same direction, what does a high score q·k mean?",
            "motivation": "Connecting the math to the semantic geometry closes the loop on why attention works.",
            "accepts": {
              "mode": "math",
              "acceptedAnswers": [
                "q and k point in similar directions, so they are semantically related",
                "the query and key are semantically close / aligned",
                "small angle between q and k means high similarity"
              ]
            },
            "hints": [
              "High cosine similarity ↔ small angle ↔ vectors pointing in the same direction ↔ semantically related by the distributional hypothesis."
            ],
            "reveal": "A high dot product score q·k means q and k point in similar directions in the embedding space — they are semantically related by the distributional hypothesis. Attention is therefore computing, for each query, how semantically relevant each key is, then doing a weighted average of the values. The geometric structure of the embedding space is the direct substrate of the attention mechanism."
          }
        ],
        "finalResult": "u·v = ||u|| ||v|| cos(θ). For unit vectors: dot product = cos(θ) = cosine similarity. Attention scores q·k measure the semantic angle between query and key — high score means semantically close, low score means unrelated. The distributional geometry learned during training is what makes this meaningful."
      }
    },
    {
      "kind": "question",
      "question": {
        "id": "emb_scen1",
        "kind": "scenario",
        "question": "Analogy arithmetic and the OOV problem",
        "prompt": "You are working with a pre-trained word embedding model. A colleague demonstrates the famous analogy: king − man + woman ≈ queen (Mikolov et al., 2013), and asks two follow-up questions. (1) A new word 'GPT-5' was coined after the model's training cutoff and has no embedding. How would you represent this out-of-vocabulary (OOV) token in practice? Name at least two strategies and their tradeoffs. (2) The analogy king − man + woman ≈ queen works, but doctor − man + woman gives a result closer to 'nurse' than 'doctor' (female). What does this reveal about how distributional training encodes societal bias, and what are the implications for using these embeddings in a production system?"
      }
    },
    {
      "kind": "section",
      "title": "Part 3 — Embeddings inside the transformer",
      "body": "Embeddings are the *start*, not the destination. Everything downstream is a transformation of this vector field."
    },
    {
      "kind": "text",
      "title": "Tied weights and the output head",
      "body": "Many transformers **tie** the input embedding matrix to the output projection: the same $E$ that turned tokens into vectors is reused (transposed) at the end to turn the final vector back into vocabulary logits. This shares parameters and tends to improve quality.\n\nSo the embedding matrix is doing double duty — encoding meaning on the way in *and* scoring tokens on the way out. A subtle consequence: improving how a token is represented also improves how confidently it can be predicted."
    },
    {
      "kind": "text",
      "title": "Contextual vs static",
      "body": "The embedding-table vector is **static** — `bank` gets one vector whether it's a river bank or a money bank. The transformer's job is to make representations **contextual**: each attention layer mixes in information from surrounding tokens so that, by the upper layers, `bank` in *\"river bank\"* and *\"savings bank\"* have moved to different places.\n\nThat's the bridge to the rest of the path: the static embedding is the raw material; attention is the machine that contextualizes it."
    },
    {
      "kind": "question",
      "question": {
        "id": "emb_q3",
        "kind": "multiple_choice",
        "question": "In a transformer, where does the meaning of the WORD 'bank' get disambiguated between 'river bank' and 'savings bank'?",
        "options": [
          "In the attention layers, which mix in surrounding-token information to make the representation contextual",
          "In the embedding table — it stores both senses",
          "In the tokenizer, which splits the two senses",
          "It is never disambiguated; the model guesses"
        ],
        "correctIndex": 0,
        "explanation": "The embedding table is static (one vector per token id). Disambiguation happens because attention layers blend in context (river vs savings), moving the token's representation toward the right sense as it flows up the stack."
      }
    },
    {
      "kind": "section",
      "title": "Recap"
    },
    {
      "kind": "text",
      "title": "What to carry forward",
      "body": "- An **embedding** places each token in a learned vector space where **direction encodes meaning** (cosine similarity).\n- The structure is **learned** from context (distributional hypothesis), not designed.\n- The embedding matrix is often **tied** to the output head.\n- Table embeddings are **static**; attention makes them **contextual**.\n\n**Next:** [Positional encoding](/wiki/positional-encoding) — injecting word *order* into these vectors — then [attention](/wiki/attention), which computes the dot-product geometry you just learned."
    }
  ]
}
