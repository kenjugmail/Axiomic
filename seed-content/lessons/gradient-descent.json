{
  "slides": [
    {
      "kind": "text",
      "title": "The basic algorithm",
      "body": "**Gradient descent** is the algorithm that powers training. Given a loss function `L(W)`, repeatedly:\n\n1. Compute the gradient `g = ∇_W L(W)`\n2. Update weights: `W ← W - η · g`\n3. Repeat\n\nWhere `η` (eta) is the **learning rate** — how big a step to take.\n\nIntuition: the gradient points in the direction of steepest *increase* of the loss; we step in the *opposite* direction to decrease it. With a small enough step size, each iteration reduces loss until we reach a minimum.\n\nFor neural networks, the gradient is computed via [[backpropagation]] — the chain rule applied through the network's computation graph. The optimizer's job is to use that gradient effectively."
    },
    {
      "kind": "text",
      "title": "Worked example: GD on a quadratic",
      "body": "Minimize $L(\\theta) = \\tfrac12 (\\theta - 3)^2$ from $\\theta_0 = 0$, learning rate $\\eta = 0.5$.\n\nGradient: $L'(\\theta) = \\theta - 3$. Update: $\\theta_{t+1} = \\theta_t - \\eta L'(\\theta_t)$.\n\n- $\\theta_0 = 0 \\Rightarrow L' = -3 \\Rightarrow \\theta_1 = 0 - 0.5 \\cdot (-3) = 1.5$.\n- $\\theta_1 = 1.5 \\Rightarrow L' = -1.5 \\Rightarrow \\theta_2 = 1.5 + 0.75 = 2.25$.\n- $\\theta_2 = 2.25 \\Rightarrow \\theta_3 = 2.625$.\n- ...\n\nEach step closes half the gap to the optimum at $\\theta^* = 3$. The remaining distance is $|\\theta_t - 3| = 3 \\cdot 0.5^t$ — exponential convergence at rate $0.5$. For quadratic objectives with $\\eta = 1/L''$, GD halves the error each step. Real losses aren't quadratic but locally behave that way near the minimum."
    },
    {
      "kind": "question",
      "question": {
        "id": "gd_gd_taylor",
        "kind": "guided_derivation",
        "question": "Derive the GD update from first-order Taylor",
        "goal": "Starting from a Taylor expansion of $L(\\theta)$, derive $\\theta_{t+1} = \\theta_t - \\eta \\nabla L(\\theta_t)$.",
        "steps": [
          {
            "prompt": "First-order Taylor of $L$ around $\\theta_t$ in the direction of a step $\\Delta\\theta$: $L(\\theta_t + \\Delta\\theta) \\approx ?$",
            "motivation": "Linear approximation gives a local model of how $L$ changes.",
            "accepts": {
              "mode": "math",
              "acceptedAnswers": [
                "L(theta_t) + grad L(theta_t) . delta theta",
                "L(θ_t) + ∇L(θ_t)·Δθ",
                "L + ∇L dot Δθ"
              ]
            },
            "hints": [
              "First-order Taylor: function plus gradient times step."
            ],
            "reveal": "$L(\\theta_t + \\Delta\\theta) \\approx L(\\theta_t) + \\nabla L(\\theta_t)^T \\Delta\\theta$."
          },
          {
            "prompt": "Among all $\\Delta\\theta$ with $\\|\\Delta\\theta\\| = \\eta$, which direction makes $\\nabla L^T \\Delta\\theta$ most negative (= biggest decrease in $L$)?",
            "motivation": "Maximize the decrease by going opposite to the gradient.",
            "accepts": {
              "mode": "choice",
              "options": [
                "$\\Delta\\theta = -\\eta \\nabla L(\\theta_t)/\\|\\nabla L(\\theta_t)\\|$",
                "$\\Delta\\theta = +\\eta \\nabla L(\\theta_t)/\\|\\nabla L(\\theta_t)\\|$",
                "$\\Delta\\theta = 0$",
                "$\\Delta\\theta = \\eta \\hat e_1$"
              ],
              "correctIndex": 0
            },
            "hints": [
              "The most-negative dot product is achieved when $\\Delta\\theta$ is opposite to the gradient.",
              "Magnitude is constrained to $\\eta$; only direction matters."
            ],
            "reveal": "$\\Delta\\theta = -\\eta\\,\\nabla L(\\theta_t)/\\|\\nabla L(\\theta_t)\\|$ (unit-norm); equivalently, $\\Delta\\theta = -\\eta \\nabla L(\\theta_t)$ when we absorb the magnitude into $\\eta$."
          }
        ],
        "finalResult": "$$\\boxed{\\theta_{t+1} = \\theta_t - \\eta \\nabla L(\\theta_t)}$$\n\nGD as 'go downhill in the steepest direction'. Higher-order methods (Newton: $\\theta_{t+1} = \\theta_t - (\\nabla^2 L)^{-1}\\nabla L$) use curvature for faster convergence at higher per-step cost. Adam, RMSprop, and momentum methods are all first-order variants with smarter step-size adaptation."
      }
    },
    {
      "kind": "text",
      "title": "Stochastic gradient descent (SGD)",
      "body": "Computing the gradient over the entire dataset at each step is expensive. **Stochastic gradient descent** uses minibatches: at each step, compute the gradient over a small random subset (a batch) of the data.\n\nThe minibatch gradient is a **noisy estimate** of the true gradient. Steps are noisier than 'true' gradient descent — but you take many more steps per unit of compute, and the noise itself can help escape shallow local minima.\n\nKey property: **SGD steps are individually noisy but trend toward the same minimum on average**. If you watch the loss curve, you see noise on top of a smooth trend. The noise is not a bug; it's the signature of stochastic optimization.\n\nMisconception: 'training loss should decrease every step.' Wrong. SGD's per-step gradient is a noisy estimator; per-step loss can go up. What matters is the *trend* on a moving average over many steps."
    },
    {
      "kind": "text",
      "title": "Momentum",
      "body": "Vanilla SGD takes the gradient of the current minibatch as its update direction. **Momentum** improves this by maintaining a running average of past gradients:\n\n$$v_t = \\beta v_{t-1} + (1 - \\beta) g_t$$\n\n$$W \\leftarrow W - \\eta v_t$$\n\nWith `β = 0.9`, the velocity `v` accumulates ~10 steps of gradient direction. Why this helps:\n\n- **Long valleys**: if the loss surface has a long valley with steep walls, vanilla SGD bounces between the walls without making progress along the valley. Momentum accumulates speed in the consistent direction (along the valley) and damps the oscillating component.\n- **Saddle points**: at a saddle point, the gradient is zero but the surface is not at a minimum. Momentum lets the optimizer 'roll through' saddle points using accumulated speed.\n\nMisconception: 'momentum is just a moving average of gradients.' Not quite — momentum maintains a *velocity* that gets carried forward; a pure moving average of gradients doesn't track previous velocity, just past gradient values."
    },
    {
      "kind": "question",
      "question": {
        "id": "gd_q1",
        "kind": "multiple_choice",
        "question": "If SGD's per-step loss spikes upward but the moving average is still falling, is training broken?",
        "options": [
          "Yes — every step should decrease loss in healthy training",
          "No — SGD's per-batch gradient is a noisy estimator; per-step jitter is normal. What matters is the trend, not individual steps",
          "Only if the spike is more than 5%",
          "Only with small learning rates"
        ],
        "correctIndex": 1,
        "explanation": "Per-step jitter is intrinsic to SGD. The gradient on one batch is a noisy estimate of the true gradient over the full dataset. Healthy training is noisy on top of a downward trend, not monotonically decreasing. Sustained upward trends are real failures; per-step bumps are not."
      }
    },
    {
      "kind": "text",
      "title": "Adam — the modern default",
      "body": "**Adam** (Adaptive Moment Estimation, Kingma & Ba 2014) is the dominant optimizer for deep learning. It maintains two running averages:\n\n- First moment `m_t` (running average of gradients, like momentum)\n- Second moment `v_t` (running average of squared gradients, capturing per-parameter scale)\n\nThe update rule:\n\n$$W \\leftarrow W - \\eta \\cdot \\frac{m_t}{\\sqrt{v_t} + \\epsilon}$$\n\nThe `1/√v_t` term **adapts the effective learning rate per parameter**. Parameters with consistently large gradients get smaller effective steps; parameters with small gradients get larger effective steps.\n\nWhy this matters: in deep networks, different parameters have wildly different gradient magnitudes. A single global learning rate either steps too aggressively for some parameters (causing instability) or too cautiously for others (slow learning). Adam's per-parameter scaling fixes this.\n\n**AdamW** is the variant universally used today. It decouples weight decay from the gradient update — addresses a subtle bug in vanilla Adam where weight decay's effective strength varies per parameter."
    },
    {
      "kind": "text",
      "title": "Learning rate schedules",
      "body": "Even with adaptive optimizers, the global learning rate `η` matters. Common patterns:\n\n**Constant LR**: simple but suboptimal. Fine for short runs.\n\n**Step decay**: drop LR by 10× at fixed step counts. Old-school but effective.\n\n**Cosine decay** (modern default): smoothly decay from initial LR to ~0 over the entire training run. Used by GPT-2 onward, LLaMA, most modern recipes.\n\n**Warmup + cosine**: linearly increase LR from 0 to peak over the first ~5-10% of steps, then cosine decay. Critical for transformer training — without warmup, large initial gradients destabilize the model. With warmup + cosine, training is reliably stable.\n\n**One-cycle**: warmup → high LR → cooldown to ~0. Used in CV; less common for LLMs.\n\nCommon misconception: 'larger learning rate = faster convergence.' The actual relationship is U-shaped — too small and you crawl, too large and you bounce off the walls or diverge. The optimal LR depends on local curvature; for transformers, the optimal LR shrinks as the model grows. See [[scaling-laws]] for the empirical relationships."
    },
    {
      "kind": "question",
      "question": {
        "id": "gd_q2",
        "kind": "multiple_choice",
        "question": "Why does training with Adam still benefit from learning rate warmup?",
        "options": [
          "Adam doesn't need warmup",
          "Adam adapts the per-parameter scale, but it does NOT eliminate the role of the global LR. At training start, Adam's running averages are not yet stable; large initial steps with the global LR can destabilize the model. Warmup gives the running averages time to settle",
          "Warmup is only needed for SGD",
          "Adam requires it for the second-moment estimate"
        ],
        "correctIndex": 1,
        "explanation": "Adam's adaptivity is per-parameter (the `1/√v_t` term). The global LR scaling still matters. At training start, the second-moment estimate `v_t` hasn't accumulated enough data; the adaptive scaling is unreliable. Warmup gives those running averages a few thousand steps to stabilize before taking large steps. The misconception that Adam removes the need for LR tuning is one of the most common in modern ML."
      }
    },
    {
      "kind": "text",
      "title": "What's next",
      "body": "Gradient descent is the engine of modern ML training. The basic algorithm hasn't changed since the 1960s; what has changed is the optimizer (Adam → AdamW), the schedule (cosine + warmup), and the scale of the data + parameters.\n\nKey takeaways:\n- SGD is gradient descent on minibatches; per-step noise is normal\n- Momentum maintains velocity across steps; helps escape shallow minima and traverse long valleys\n- Adam adapts per-parameter scale; AdamW with cosine + warmup is the modern default\n- Learning rate matters; the right value depends on model size, data, and schedule\n\nNext: [[backpropagation]] — how gradients are computed efficiently in deep networks."
    },
    {
      "kind": "text",
      "title": "Why momentum exists",
      "body": "Vanilla gradient descent oscillates badly in narrow valleys: the gradient zig-zags across the valley walls while making slow progress along the floor.\n\nMomentum keeps a running average $\\mathbf{v}_t = \\beta\\,\\mathbf{v}_{t-1} + (1-\\beta)\\,\\nabla L(\\theta_t)$ and updates $\\theta_{t+1} = \\theta_t - \\eta\\,\\mathbf{v}_t$. The cross-valley components cancel out of the average; the along-the-floor component reinforces. The result is straighter, faster descent.\n\nAdam takes the same idea and adds per-parameter learning-rate scaling via the running variance of gradients — that's the trick that lets transformer training proceed without manual learning-rate tuning per layer."
    },
    {
      "kind": "question",
      "question": {
        "id": "mc_gd_lr",
        "kind": "multiple_choice",
        "question": "During training, the loss decreases for the first few epochs and then starts oscillating between $0.4$ and $1.8$ without settling. Which intervention is most likely to help?",
        "options": [
          "Increase the learning rate.",
          "Decrease the learning rate.",
          "Increase the batch size — but keep the learning rate the same.",
          "Switch from SGD to vanilla gradient descent."
        ],
        "correctIndex": 1,
        "explanation": "Oscillation around a minimum is the classic signature of a learning rate that's too high — the optimizer overshoots and bounces. Lowering $\\eta$ (or adding a learning-rate schedule that decays it) is the first thing to try."
      },
      "hints": [
        "Map each symptom (oscillation, slow progress, plateau, divergence) to a learning-rate diagnosis.",
        "Increasing batch size reduces noise but doesn't reduce step size."
      ],
      "workedSolution": "Oscillation around a minimum without settling is the textbook 'learning rate too high' symptom: each step overshoots the basin. Decreasing $\\eta$ (or using a decay schedule) cures it. Increasing the LR makes it worse; raising the batch size reduces gradient noise but doesn't shrink the step size.",
      "retryUntilCorrect": true
    },
    {
      "kind": "question",
      "question": {
        "id": "math_gd_step",
        "kind": "math_expression",
        "question": "You're minimizing $L(\\theta) = \\theta^2$ with $\\theta_0 = 4$ and learning rate $\\eta = 0.1$. What is $\\theta_1$ after one gradient-descent step?",
        "acceptedAnswers": [
          "3.2",
          "3.20",
          "16/5"
        ],
        "hint": "Gradient is $2\\theta$, so $\\theta_1 = \\theta_0 - \\eta\\cdot 2\\theta_0$."
      }
    },
    {
      "kind": "question",
      "question": {
        "id": "scn_gd_plateau",
        "kind": "scenario",
        "question": "A long flat loss curve",
        "prompt": "Training loss drops sharply for one epoch, then sits at a plateau for fifty epochs before slowly decreasing again. What two distinct causes could explain that pattern, and how would you tell them apart?"
      }
    },
    {
      "kind": "explain_back",
      "question": {
        "id": "eb_gd",
        "kind": "explain_back",
        "prompt": "Explain in your own words, in two sentences, why Adam often 'just works' on transformer training when vanilla SGD requires careful tuning.",
        "rubric": "Mentions per-parameter step-size scaling via running gradient variance, and that this implicitly handles bad conditioning across layers."
      }
    }
  ],
  "meta": {
    "timeMinutes": 30,
    "difficulty": "core",
    "objectives": [
      "State the gradient-descent update and reason about step-size choice.",
      "Distinguish vanilla GD, SGD, momentum, and Adam by their update rules.",
      "Recognize loss-curve symptoms of a too-large or too-small learning rate."
    ],
    "prereqs": [
      "calculus-foundations"
    ]
  }
}
