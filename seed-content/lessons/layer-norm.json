{
  "meta": {
    "timeMinutes": 20,
    "difficulty": "core",
    "objectives": [
      "Explain what LayerNorm normalizes and why",
      "Contrast pre-norm vs post-norm placement"
    ],
    "prereqs": []
  },
  "slides": [
    {
      "kind": "section",
      "title": "Part 1 — Keeping a deep stack stable",
      "body": "Stack 50 layers without normalization and activations drift by orders of magnitude; the loss plateaus and nothing trains. Layer normalization is one of the two tricks (the other is residuals) that make deep transformers possible at all."
    },
    {
      "kind": "text",
      "title": "What layer norm does",
      "body": "For each token's vector, **per token, across the feature dimension**:\n\n1. Compute mean $\\mu$ and std $\\sigma$ over the $d_{model}$ features.\n2. Normalize: subtract $\\mu$, divide by $\\sigma$.\n3. Apply learned scale $\\gamma$ and shift $\\beta$:\n\n$$\\text{LN}(x)=\\gamma\\cdot\\frac{x-\\mu}{\\sigma+\\epsilon}+\\beta$$\n\nThe activation panel on the right shows what unnormalized vs normalized activations look like across a layer — the normalized ones stay on a stable scale instead of drifting. The defining property: LN is computed **per token**, independent of batch and sequence length.",
      "viz": "layer-activations"
    },
    {
      "kind": "text",
      "title": "Layer norm vs batch norm",
      "body": "Both normalize, over **different axes**:\n\n- **Batch norm:** per *feature*, across the *batch*.\n- **Layer norm:** per *example/token*, across its *features*.\n\nWhy it matters for transformers:\n\n- **Inference parity.** Batch norm uses per-batch stats at train time and running averages at inference — forget to switch and predictions silently corrupt. Layer norm is identical in both.\n- **Variable length.** Transformers process variable-length, padded sequences; batch norm's batch stats get confused, layer norm doesn't care."
    },
    {
      "kind": "question",
      "question": {
        "id": "ln_q1",
        "kind": "multiple_choice",
        "question": "For an input of shape [batch, seq_len, d_model], which axis does layer norm reduce over?",
        "options": [
          "d_model — per-token, per-example",
          "batch",
          "seq_len",
          "all three"
        ],
        "correctIndex": 0,
        "explanation": "For each (batch, seq position), LN computes mean/std across the d_model feature axis. That batch- and length-independence is exactly why it behaves identically at training and inference — the whole reason it replaced batch norm in transformers."
      }
    },
    {
      "kind": "question",
      "question": {
        "id": "ln_q2",
        "kind": "multiple_choice",
        "question": "A model trained with batch norm is served at batch_size = 1 in training mode. What breaks?",
        "options": [
          "Per-batch stats become meaningless (the 'mean' is the single input); only running-average eval mode is correct — forgetting to switch silently corrupts outputs",
          "Nothing — batch norm is batch-size independent",
          "It just runs faster",
          "Activations always explode regardless of mode"
        ],
        "correctIndex": 0,
        "explanation": "Batch norm has two stat sources: per-batch (train) and running averages (eval). At batch_size = 1 the per-batch 'statistics' are degenerate. `model.eval()` switches to running averages; forgetting it is a classic deployment bug. Layer norm sidesteps it entirely."
      }
    },
    {
      "kind": "section",
      "title": "Part 2 — Placement and the modern variant"
    },
    {
      "kind": "text",
      "title": "Pre-norm vs post-norm",
      "body": "Where LN sits relative to the residual add matters at depth:\n\n**Post-norm** (original): `out = LN(x + Sublayer(x))`\n\n**Pre-norm** (GPT-2 onward): `out = x + Sublayer(LN(x))`\n\nIn pre-norm the residual stream itself is never normalized — the clean identity path is preserved, so very deep stacks train stably. Post-norm normalizes the stream every layer, which destabilizes deep models. Every modern large model uses **pre-norm**; post-norm is mostly historical (it can edge out the final loss when it *does* train, but pre-norm is the safe default)."
    },
    {
      "kind": "text",
      "title": "RMSNorm",
      "body": "**RMSNorm** drops the mean-subtraction, normalizing only the magnitude:\n\n$$\\text{RMSNorm}(x)=\\gamma\\cdot\\frac{x}{\\sqrt{\\tfrac{1}{d}\\sum_i x_i^2}}$$\n\nEmpirically the mean-centering term contributes little for transformers, so RMSNorm matches or beats LayerNorm at slightly less compute. LLaMA, Mistral, and most modern open models use it. If a model card says `RMSNorm`, read it as \"LayerNorm minus the recentering.\""
    },
    {
      "kind": "question",
      "question": {
        "id": "ln_q3",
        "kind": "multiple_choice",
        "question": "Why did large models migrate from post-norm to pre-norm?",
        "options": [
          "Pre-norm leaves the residual stream un-normalized, preserving the identity path so very deep stacks train stably",
          "Pre-norm uses fewer parameters",
          "Post-norm cannot represent the same functions",
          "Pre-norm removes the need for residual connections"
        ],
        "correctIndex": 0,
        "explanation": "Pre-norm normalizes only the *input* to each sublayer; the residual stream flows through unnormalized, keeping a clean gradient highway across depth. Post-norm renormalizes the stream every layer, which causes instability as depth grows — fatal for 50–100 layer models."
      }
    },
    {
      "kind": "section",
      "title": "Recap"
    },
    {
      "kind": "question",
      "question": {
        "id": "ln_derive",
        "kind": "guided_derivation",
        "question": "Derive LayerNorm",
        "goal": "From: stabilize each token's activations independent of the batch.",
        "steps": [
          {
            "prompt": "Normalize over which axis?",
            "motivation": "Per-token over the feature axis makes it batch-size independent — works the same at batch 1 or 1024.",
            "accepts": {
              "mode": "choice",
              "options": [
                "The feature dimension, per token",
                "The batch dimension",
                "The whole tensor at once",
                "The time axis only"
              ],
              "correctIndex": 0
            },
            "hints": [
              "It must not depend on other examples in the batch."
            ],
            "reveal": "LayerNorm normalizes each token vector over its feature dimension."
          },
          {
            "prompt": "Write LayerNorm(x) (use mean μ, var σ², ε, scale γ, shift β).",
            "motivation": "Standardize then re-introduce learnable scale/shift so the layer can still represent any mean/variance it needs.",
            "accepts": {
              "mode": "math",
              "acceptedAnswers": [
                "gamma*(x-mu)/sqrt(sigma^2+eps)+beta",
                "\\gamma\\frac{x-\\mu}{\\sqrt{\\sigma^2+\\epsilon}}+\\beta",
                "(x-mu)/sqrt(var+eps)*gamma+beta"
              ]
            },
            "hints": [
              "Standardize first, then affine with γ, β."
            ],
            "reveal": "LN(x) = γ·(x−μ)/√(σ²+ε) + β."
          }
        ],
        "finalResult": "$$\\text{LN}(x)=\\gamma\\,\\frac{x-\\mu}{\\sqrt{\\sigma^2+\\epsilon}}+\\beta$$"
      }
    },
    {
      "kind": "question",
      "question": {
        "id": "ln_scale",
        "kind": "ml_sandbox",
        "question": "Blow the input scale up to 100× — LayerNorm output std should stay ≈ 1 (target ≤ 1.5).",
        "params": [
          {
            "name": "scale",
            "label": "input scale",
            "min": 0.1,
            "max": 100,
            "step": 0.1,
            "default": 1
          }
        ],
        "harnessCode": "import numpy as np\nnp.random.seed(0)\nx=np.random.randn(64,256)*float(scale)\nmu=x.mean(1,keepdims=True); var=x.var(1,keepdims=True)\ny=(x-mu)/np.sqrt(var+1e-5)\nmetrics={'out_std':float(y.std())}",
        "target": {
          "metric": "out_std",
          "op": "lte",
          "value": 1.5
        },
        "explanation": "Whatever the input scale, LN renormalizes each token to ~unit variance — that scale-invariance is the whole point."
      }
    },
    {
      "kind": "explain_back",
      "question": {
        "id": "ln_teach",
        "kind": "explain_back",
        "prompt": "Explain LayerNorm to your pet: what it normalizes, why per-token, and what γ/β are for.",
        "rubricCriteria": [
          {
            "id": "c1",
            "description": "normalizes each token over its feature dimension"
          },
          {
            "id": "c2",
            "description": "is independent of batch size (unlike BatchNorm)"
          },
          {
            "id": "c3",
            "description": "γ and β are learnable scale/shift restoring expressivity"
          }
        ]
      }
    },
    {
      "kind": "text",
      "title": "What to carry forward",
      "body": "- LN normalizes **per token across features** → batch/length independent, identical at train & inference.\n- Not batch norm — different axis, no eval-mode footgun.\n- **Pre-norm** placement is the modern default for trainable depth; **RMSNorm** is the common simplification.\n\n**Next:** [Residual connections](/wiki/residual-connections) — the other half of \"why deep transformers train\" — then the full [transformer block](/wiki/transformer-block)."
    }
  ]
}
