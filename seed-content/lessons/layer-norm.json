{
  "meta": {
    "timeMinutes": 35,
    "difficulty": "core",
    "objectives": [
      "Explain what LayerNorm normalizes and why",
      "Contrast pre-norm vs post-norm placement"
    ],
    "prereqs": []
  },
  "slides": [
    {
      "kind": "section",
      "title": "Part 1 — Why normalization exists",
      "body": "Stack 24 blocks naively and activations drift/explode. Normalization is the fix that makes depth trainable."
    },
    {
      "kind": "text",
      "title": "The problem: activation drift with depth",
      "body": "Each sublayer adds to the residual stream; without normalization the activation scale wanders and compounds over many layers, pushing nonlinearities into saturated regions and gradients toward 0 or ∞. Training becomes hyper-sensitive to init and learning rate. We need to re-stabilize the scale *at every layer*."
    },
    {
      "kind": "text",
      "title": "What LayerNorm does",
      "body": "For each token vector independently, LayerNorm standardizes over the **feature** dimension then applies a learned affine: $\\text{LN}(x)=\\gamma\\,\\dfrac{x-\\mu}{\\sqrt{\\sigma^2+\\epsilon}}+\\beta$, with $\\mu,\\sigma^2$ the mean/variance across that token's features. $\\gamma,\\beta$ let the layer re-expand to whatever scale/shift it actually needs.",
      "viz": "layer-activations"
    },
    {
      "kind": "text",
      "title": "LayerNorm vs BatchNorm",
      "body": "BatchNorm normalizes each feature across the *batch* — its statistics depend on batch size and other examples, which breaks for variable-length sequences and tiny inference batches. LayerNorm is **per-token, batch-independent**: identical behavior at batch 1 or 1024, train or serve. That independence is why transformers use it.",
      "viz": "layer-activations"
    },
    {
      "kind": "question",
      "question": {
        "id": "ln_q1",
        "kind": "multiple_choice",
        "question": "For an input of shape [batch, seq_len, d_model], which axis does layer norm reduce over?",
        "options": [
          "d_model — per-token, per-example",
          "batch",
          "seq_len",
          "all three"
        ],
        "correctIndex": 0,
        "explanation": "For each (batch, seq position), LN computes mean/std across the d_model feature axis. That batch- and length-independence is exactly why it behaves identically at training and inference — the whole reason it replaced batch norm in transformers."
      }
    },
    {
      "kind": "question",
      "question": {
        "id": "ln_q2",
        "kind": "multiple_choice",
        "question": "A model trained with batch norm is served at batch_size = 1 in training mode. What breaks?",
        "options": [
          "Per-batch stats become meaningless (the 'mean' is the single input); only running-average eval mode is correct — forgetting to switch silently corrupts outputs",
          "Nothing — batch norm is batch-size independent",
          "It just runs faster",
          "Activations always explode regardless of mode"
        ],
        "correctIndex": 0,
        "explanation": "Batch norm has two stat sources: per-batch (train) and running averages (eval). At batch_size = 1 the per-batch 'statistics' are degenerate. `model.eval()` switches to running averages; forgetting it is a classic deployment bug. Layer norm sidesteps it entirely."
      }
    },
    {
      "kind": "section",
      "title": "Part 2 — Formalize it"
    },
    {
      "kind": "question",
      "question": {
        "id": "ln_derive",
        "kind": "guided_derivation",
        "question": "Derive LayerNorm",
        "goal": "From: stabilize each token's activations independent of the batch.",
        "steps": [
          {
            "prompt": "Normalize over which axis?",
            "motivation": "Per-token over the feature axis makes it batch-size independent — works the same at batch 1 or 1024.",
            "accepts": {
              "mode": "choice",
              "options": [
                "The feature dimension, per token",
                "The batch dimension",
                "The whole tensor at once",
                "The time axis only"
              ],
              "correctIndex": 0
            },
            "hints": [
              "It must not depend on other examples in the batch."
            ],
            "reveal": "LayerNorm normalizes each token vector over its feature dimension."
          },
          {
            "prompt": "Write LayerNorm(x) (use mean μ, var σ², ε, scale γ, shift β).",
            "motivation": "Standardize then re-introduce learnable scale/shift so the layer can still represent any mean/variance it needs.",
            "accepts": {
              "mode": "math",
              "acceptedAnswers": [
                "gamma*(x-mu)/sqrt(sigma^2+eps)+beta",
                "\\gamma\\frac{x-\\mu}{\\sqrt{\\sigma^2+\\epsilon}}+\\beta",
                "(x-mu)/sqrt(var+eps)*gamma+beta"
              ]
            },
            "hints": [
              "Standardize first, then affine with γ, β."
            ],
            "reveal": "LN(x) = γ·(x−μ)/√(σ²+ε) + β."
          }
        ],
        "finalResult": "$$\\text{LN}(x)=\\gamma\\,\\frac{x-\\mu}{\\sqrt{\\sigma^2+\\epsilon}}+\\beta$$"
      }
    },
    {
      "kind": "question",
      "question": {
        "id": "ln_scale",
        "kind": "ml_sandbox",
        "question": "Blow the input scale up to 100× — LayerNorm output std should stay ≈ 1 (target ≤ 1.5).",
        "params": [
          {
            "name": "scale",
            "label": "input scale",
            "min": 0.1,
            "max": 100,
            "step": 0.1,
            "default": 1
          }
        ],
        "harnessCode": "import numpy as np\nnp.random.seed(0)\nx=np.random.randn(64,256)*float(scale)\nmu=x.mean(1,keepdims=True); var=x.var(1,keepdims=True)\ny=(x-mu)/np.sqrt(var+1e-5)\nmetrics={'out_std':float(y.std())}",
        "target": {
          "metric": "out_std",
          "op": "lte",
          "value": 1.5
        },
        "explanation": "Whatever the input scale, LN renormalizes each token to ~unit variance — that scale-invariance is the whole point."
      }
    },
    {
      "kind": "section",
      "title": "Part 3 — Placement & the modern variant"
    },
    {
      "kind": "text",
      "title": "Pre-norm vs post-norm",
      "body": "Original transformers put LN *after* the residual add (**post-norm**) and needed careful LR warmup or they diverged. Modern deep models use **pre-norm** — LN *inside* the residual branch, $x + \\text{Sublayer}(\\text{LN}(x))$ — which leaves the identity path un-normalized so gradient flows cleanly through depth without warmup."
    },
    {
      "kind": "text",
      "title": "RMSNorm",
      "body": "Most recent LLMs drop the mean-centering and the $\\beta$ shift, normalizing only by the root-mean-square: $\\text{RMSNorm}(x)=\\gamma\\,x/\\sqrt{\\overline{x^2}+\\epsilon}$. Cheaper, fewer params, empirically as good — the current default (Llama, etc.)."
    },
    {
      "kind": "question",
      "question": {
        "id": "ln_q3",
        "kind": "multiple_choice",
        "question": "Why did large models migrate from post-norm to pre-norm?",
        "options": [
          "Pre-norm leaves the residual stream un-normalized, preserving the identity path so very deep stacks train stably",
          "Pre-norm uses fewer parameters",
          "Post-norm cannot represent the same functions",
          "Pre-norm removes the need for residual connections"
        ],
        "correctIndex": 0,
        "explanation": "Pre-norm normalizes only the *input* to each sublayer; the residual stream flows through unnormalized, keeping a clean gradient highway across depth. Post-norm renormalizes the stream every layer, which causes instability as depth grows — fatal for 50–100 layer models."
      }
    },
    {
      "kind": "section",
      "title": "Part 4 — Pitfalls & misconceptions"
    },
    {
      "kind": "question",
      "question": {
        "id": "ln_scen",
        "kind": "scenario",
        "question": "Diagnose the training instability.",
        "scenario": "Moving LayerNorm from pre-norm (inside the residual branch) to post-norm (after the residual add), a deep model now diverges early unless you add careful LR warmup.",
        "rubricCriteria": [
          {
            "id": "c1",
            "description": "post-norm normalizes x+F(x), so the identity/residual gradient path is renormalized every layer"
          },
          {
            "id": "c2",
            "description": "that degrades the clean gradient highway with depth → early divergence without warmup"
          },
          {
            "id": "c3",
            "description": "pre-norm normalizes only F's input, leaving the identity path intact → stable deep training (modern default)"
          }
        ],
        "explanation": "Norm placement relative to the residual add decides whether the identity gradient path stays clean. Pre-norm keeps it clean; post-norm doesn't."
      }
    },
    {
      "kind": "question",
      "hints": [
        "Where is LN relative to the x + F(x) add?",
        "Which placement leaves the identity path un-normalized?"
      ],
      "workedSolution": "Pre-norm normalizes only F's input, so the residual identity path carries gradient undiminished through many layers — deep training is stable without warmup. Post-norm normalizes the sum, eroding that path.",
      "retryUntilCorrect": true,
      "question": {
        "id": "ln_mc",
        "kind": "multiple_choice",
        "question": "Why do modern deep transformers prefer pre-norm (LN inside the residual branch)?",
        "options": [
          "It preserves a clean identity gradient path, so very deep models train stably without heavy warmup",
          "Post-norm has fewer parameters",
          "Pre-norm removes the need for residuals",
          "They are identical"
        ],
        "correctIndex": 0,
        "explanation": "Pre-norm: out = x + F(LN(x)) — the x path is never renormalized, so its gradient stays ~1 through depth. Post-norm renormalizes x+F(x) each layer."
      }
    },
    {
      "kind": "section",
      "title": "Part 5 — Connections & what's next"
    },
    {
      "kind": "explain_back",
      "question": {
        "id": "ln_teach",
        "kind": "explain_back",
        "prompt": "Explain LayerNorm to your pet: what it normalizes, why per-token, and what γ/β are for.",
        "rubricCriteria": [
          {
            "id": "c1",
            "description": "normalizes each token over its feature dimension"
          },
          {
            "id": "c2",
            "description": "is independent of batch size (unlike BatchNorm)"
          },
          {
            "id": "c3",
            "description": "γ and β are learnable scale/shift restoring expressivity"
          }
        ]
      }
    },
    {
      "kind": "text",
      "title": "What to carry forward",
      "body": "LayerNorm = per-token, batch-independent re-standardization (pre-norm placement, RMSNorm variant) that keeps deep stacks stable. Its partner is the **[residual connection](/paths/ml-engineer/lessons/residual-connections)** — next."
    }
  ]
}
