{
  "slides": [
    {
      "kind": "text",
      "title": "Why estimation matters",
      "body": "Robots have sensors that measure their state — but sensors are noisy, biased, and sometimes don't observe the full state directly.\n\n**Concrete examples**:\n\n- A drone has IMU (acceleration + angular rate), GPS (position), and barometer (altitude). None directly measures velocity. To know velocity, you have to integrate IMU, but IMU drifts. To stop drift, you fuse GPS — but GPS is slow and inaccurate. The combination needs careful weighting.\n- A robot arm has joint encoders (high precision) and force-torque sensor (noisy). Estimating the load on the gripper requires combining both.\n- A self-driving car has LIDAR, cameras, radar, GPS, IMU. None alone gives accurate position; the fusion does.\n\n**Estimation** = computing the most likely state given (noisy) measurements + a model of the dynamics.\n\nThe canonical algorithm: the **Kalman filter** (1960). Optimal for linear systems with Gaussian noise. Workhorse of every modern aerospace + robotics system."
    },
    {
      "kind": "text",
      "title": "Worked example: GPS + IMU fusion",
      "body": "Phones, drones, autonomous cars all fuse GPS (~5 m accuracy, 1 Hz) with IMU (mm-scale accuracy short-term, drifts at 1°/s). Kalman filter weighs them: GPS gets ~all the weight at low frequencies (anchors absolute position); IMU gets weight at high frequencies (fast updates between GPS samples). Result: meter-scale absolute accuracy at 100+ Hz update rate. The exact gain values are computed by the Kalman covariance update — automatically optimal weighting given the noise characteristics."
    },
    {
      "kind": "question",
      "question": {
        "id": "gd_est_kgain",
        "kind": "guided_derivation",
        "question": "Derive Kalman gain as variance-weighted average",
        "goal": "1-D system. Prediction $\\hat x$ with variance $P$, measurement $z$ with variance $R$. What's the optimal weighting?",
        "steps": [
          {
            "prompt": "Posterior estimate $\\hat x' = w_1 \\hat x + w_2 z$. Posterior variance is minimized when $w_1 + w_2 = 1$ and the variances combine inversely. Solve for the gain $K = w_2$.",
            "motivation": "Variance-weighted average.",
            "accepts": {
              "mode": "math",
              "acceptedAnswers": [
                "P / (P + R)"
              ]
            },
            "hints": [
              "Inverse-variance weighting: weight on a quantity is its precision (= 1/variance)."
            ],
            "reveal": "$K = P / (P + R)$."
          },
          {
            "prompt": "Posterior variance $P' = ?$",
            "motivation": "Reduce variance after observation.",
            "accepts": {
              "mode": "math",
              "acceptedAnswers": [
                "(1-K) P",
                "P R / (P + R)"
              ]
            },
            "hints": [
              "$P' = (1 - K)P$ — gain reduces uncertainty in proportion to the trust given to the measurement."
            ],
            "reveal": "$P' = (1 - K) P = PR/(P+R)$."
          }
        ],
        "finalResult": "$$\\boxed{K = P/(P+R),\\quad P' = (1-K)P}$$ Kalman gain is the optimal inverse-variance weighting. Large $P$ (prediction uncertain) → big $K$ (trust measurement). Small $P$ → small $K$ (trust prediction). Generalized to multivariate via matrix Schur complements."
      }
    },
    {
      "kind": "text",
      "title": "Kalman filter — the recursive Bayesian update",
      "body": "Given:\n\n- Linear dynamics: `x_{k+1} = F x_k + B u_k + w_k`, `w_k ∼ N(0, Q)`.\n- Linear measurement: `z_k = H x_k + v_k`, `v_k ∼ N(0, R)`.\n\nThe Kalman filter maintains a Gaussian estimate of state `(x̂_k, P_k)`:\n\n**Predict**:\n- `x̂_{k+1|k} = F x̂_{k|k} + B u_k`\n- `P_{k+1|k} = F P_{k|k} F^T + Q`\n\n**Update** (when measurement `z_{k+1}` arrives):\n- Kalman gain: `K = P_{k+1|k} H^T (H P_{k+1|k} H^T + R)^{-1}`\n- `x̂_{k+1|k+1} = x̂_{k+1|k} + K (z_{k+1} - H x̂_{k+1|k})`\n- `P_{k+1|k+1} = (I - KH) P_{k+1|k}`\n\nThe gain `K` is the optimal weighting: trust the measurement more when measurement noise is small or prior uncertainty is large.\n\nThis is exactly Bayesian inference for the linear-Gaussian case, computed recursively (no need to keep history)."
    },
    {
      "kind": "question",
      "question": {
        "id": "est_q1",
        "kind": "multiple_choice",
        "question": "What does the Kalman gain K do as the measurement noise R increases?",
        "options": [
          "K increases",
          "K decreases — the filter trusts the measurement less when measurement noise is larger. In the limit R → ∞, K → 0, the filter ignores measurements entirely and propagates only the dynamic prediction. This is the sensor-fusion intuition: weight measurements by their reliability",
          "K stays constant",
          "K becomes complex"
        ],
        "correctIndex": 1,
        "explanation": "K trades off prior uncertainty (P) against measurement noise (R). Higher R → less weight on measurement. Higher P → more weight on measurement. The math automatically handles the right blend."
      }
    },
    {
      "kind": "text",
      "title": "Extended Kalman Filter (EKF)",
      "body": "Real systems are nonlinear. The KF assumes linear dynamics + measurement; in nonlinear cases, **linearize** at each step and apply KF formulas.\n\n**EKF procedure**:\n\n1. Linearize dynamics + measurement around current estimate: `F = ∂f/∂x|_{x̂}`, `H = ∂h/∂x|_{x̂}`.\n2. Apply KF predict + update with these Jacobians.\n3. The state estimate uses nonlinear `f` and `h`; only the covariance update uses linearized matrices.\n\n**Strengths**: workhorse of legacy aerospace + robotics. Cheap, well-understood.\n\n**Limits**:\n\n- Linearization errors accumulate; bad for highly nonlinear systems.\n- Requires Jacobians (analytic or autodiff).\n- Can diverge if linearization is poor or initial estimate is far from truth.\n\n**Modern alternatives**:\n\n- **Unscented Kalman Filter (UKF)**: propagates 'sigma points' through the nonlinear dynamics; better captures nonlinearity. No Jacobian needed.\n- **Particle filter**: Monte Carlo. Handles non-Gaussian + multimodal beliefs. More expensive but more flexible.\n- **Optimization-based estimation** (factor graphs): formulate as nonlinear least squares; solve via Levenberg-Marquardt or Gauss-Newton. Used in modern SLAM."
    },
    {
      "kind": "text",
      "title": "Particle filters",
      "body": "When dynamics or measurements are highly nonlinear, multimodal, or non-Gaussian, KF/EKF fails. **Particle filter (sequential Monte Carlo)** represents the belief as a set of weighted samples (particles).\n\n**Procedure**:\n\n1. Maintain `N` particles `{x_k^{(i)}, w_k^{(i)}}`.\n2. **Predict**: propagate each particle through nonlinear dynamics + add noise.\n3. **Update**: weight each particle by `p(z_k | x_k^{(i)})` × prior weight.\n4. **Resample**: draw `N` particles with replacement proportional to weights. Prevents weight degeneracy.\n\n**Strengths**: handles arbitrary nonlinearity, multimodality, non-Gaussian noise. Used in robotics localization (Monte Carlo Localization), tracking, recursive Bayesian inference.\n\n**Limits**:\n\n- Computational cost scales with particles + dimension. Curse of dimensionality.\n- Particle depletion: in narrow likelihood regions, most particles get zero weight; need lots of particles or careful proposal distributions.\n- Real-time use limited to low-dimensional state spaces (~10).\n\n**Modern variants**: rao-blackwellized PF (analytical marginalization for parts of the state), particle MCMC, particle smoothing."
    },
    {
      "kind": "question",
      "question": {
        "id": "est_q2",
        "kind": "multiple_choice",
        "question": "When would you use a particle filter instead of an extended Kalman filter?",
        "options": [
          "Always, particle filters are better",
          "When the state distribution is multimodal (multiple plausible hypotheses), the dynamics or measurements are highly nonlinear, or noise is non-Gaussian. Robotics localization often has multiple plausible positions until measurements disambiguate — PF handles this. EKF assumes Gaussian unimodal beliefs and would collapse to one mode prematurely",
          "When you have GPU acceleration",
          "Particle filters are obsolete"
        ],
        "correctIndex": 1,
        "explanation": "Multimodal beliefs are the classical motivation for PF. A robot in a long hallway might genuinely not know which end it's at; PF maintains both hypotheses and lets later measurements disambiguate. EKF would collapse to one mode and might commit to the wrong one."
      }
    },
    {
      "kind": "text",
      "title": "Practical considerations",
      "body": "**Sensor calibration**: estimation algorithms assume known noise statistics (Q, R). In practice, these are tuned. Allan variance for IMUs, calibration runs for cameras + LIDAR.\n\n**Time synchronization**: when fusing multiple sensors at different rates, careful timestamp alignment matters. Microsecond-level errors degrade fusion.\n\n**Outlier rejection**: real sensors have outliers (GPS multipath, LIDAR specular reflections). KF can be destabilized; use robust variants (Mahalanobis distance gating, M-estimators, MAP-based filters).\n\n**Filter health monitoring**: track innovation (`z_k - H x̂_k`) statistics. Persistent biases or growing innovations indicate filter failure or model mismatch.\n\n**Sensor failure handling**: when a sensor fails, the filter should detect + reject its measurements rather than letting bad data corrupt the state. ROS provides standard frameworks (`robot_localization`, `gtsam`).\n\n**Rule of thumb**: estimation is half the battle in robotics. Hardware that doesn't have good sensors + software for fusing them won't perform regardless of how good the planner + controller are."
    },
    {
      "kind": "explain_back",
      "question": {
        "id": "est_explain",
        "kind": "explain_back",
        "prompt": "Explain in your own words how a Kalman filter combines a noisy measurement with a noisy prediction. Use the Kalman gain to walk through the intuition.",
        "rubricCriteria": [
          {
            "id": "weighting-mechanism",
            "description": "Names that K weights the measurement vs prediction by their relative noise"
          },
          {
            "id": "gaussian-update",
            "description": "Connects to Bayesian inference (prior × likelihood ∝ posterior, all Gaussians)"
          },
          {
            "id": "limit-cases",
            "description": "Mentions a limit case (R → ∞ → ignore measurement; R → 0 → trust measurement entirely)"
          }
        ],
        "passingScore": 0.6
      }
    },
    {
      "kind": "text",
      "title": "Kalman filter, predict + update",
      "body": "**Predict step**: given current belief $(\\hat x, P)$ and input $u$:\n- $\\hat x^- = A\\hat x + Bu$ (state propagated forward)\n- $P^- = A P A^T + Q$ (covariance propagated, $Q$ is process noise)\n\n**Update step**: given measurement $z$:\n- $K = P^- C^T (C P^- C^T + R)^{-1}$ (Kalman gain)\n- $\\hat x = \\hat x^- + K(z - C\\hat x^-)$ (correct prediction with measurement residual)\n- $P = (I - KC) P^-$ (covariance reduced by measurement)\n\nIntuition: predict shifts the belief forward (uncertainty grows); update weighs the measurement against prediction by their relative confidence (uncertainty shrinks).\n\n**Optimal for linear Gaussian systems**. For nonlinear systems, options:\n- **Extended Kalman Filter (EKF)**: linearize the dynamics around current estimate. Fast; can diverge if nonlinearity is severe or covariance is large.\n- **Unscented Kalman Filter (UKF)**: deterministically sample sigma-points through the nonlinearity. More robust than EKF; same cost order.\n- **Particle filter**: sample-based; handles arbitrary nonlinearity + non-Gaussian distributions. Heavy compute but handles multimodal beliefs.\n\nReal-world choice: EKF for low-dimensional smooth nonlinearities (most robotic IMU/GPS fusion); particle filter for low-dimensional non-Gaussian (some SLAM, simultaneous localization on a multimodal map); UKF when EKF diverges and you want a drop-in replacement.\n\nDeep learning has produced hybrid methods (e.g., DeepKF, learned observation models) but the Kalman framework remains the backbone of most robotic state estimation."
    },
    {
      "kind": "question",
      "question": {
        "id": "mc_est_kalman",
        "kind": "multiple_choice",
        "question": "Kalman gain $K$ in the update step:",
        "options": [
          "Is a constant matrix tuned by hand.",
          "Trades off the prediction's uncertainty against the measurement's uncertainty.",
          "Always equals identity.",
          "Determines the time-step size.\n"
        ],
        "correctIndex": 1,
        "explanation": "Kalman gain weights how much the new measurement should pull the estimate vs. trusting the prediction. Large $K$ when measurement is more certain than prediction; small $K$ when prediction is more certain. This is the optimal trade-off in the least-squares sense for linear Gaussian systems."
      },
      "hints": [
        "The gain is computed dynamically from the prediction and measurement covariances.",
        "What does it MEAN to weight 'this measurement' vs 'my prediction'?"
      ],
      "workedSolution": "Kalman gain $K = P^- C^T (CP^- C^T + R)^{-1}$ is the optimal least-squares weighting between prediction (with covariance $P^-$) and measurement (with noise covariance $R$). Large $P^-$ relative to $R$ → big $K$ → trust the measurement. Small $P^-$ → small $K$ → trust the prediction. The gain is computed dynamically from the running covariance.",
      "retryUntilCorrect": true
    },
    {
      "kind": "question",
      "question": {
        "id": "scn_est_diverge",
        "kind": "scenario",
        "question": "EKF diverges",
        "prompt": "Your robot's EKF is running for hours and then suddenly the covariance blows up to inf and the state estimate becomes nonsensical. What's the most likely cause, and what's the standard mitigation?"
      }
    },
    {
      "kind": "question",
      "question": {
        "id": "math_est_gain",
        "kind": "math_expression",
        "question": "If prediction variance is $P^- = 4$ and measurement variance is $R = 1$ (1D), the Kalman gain $K = P^- / (P^- + R)$. Compute.",
        "acceptedAnswers": [
          "0.8",
          "4/5",
          ".8"
        ],
        "hint": "Just divide."
      }
    },
    {
      "kind": "explain_back",
      "question": {
        "id": "eb_est",
        "kind": "explain_back",
        "prompt": "Explain in two or three sentences why particle filters are more robust than Kalman filters but rarely the first choice.",
        "rubric": "Mentions that particle filters represent belief by samples (handling arbitrary non-Gaussian shapes) but cost scales with sample count to maintain accuracy; for the common case of smooth nonlinearity + roughly-Gaussian belief, EKF/UKF is much cheaper."
      }
    }
  ],
  "meta": {
    "timeMinutes": 25,
    "difficulty": "core",
    "objectives": [
      "Sketch the Kalman filter's predict + update cycle.",
      "Distinguish Kalman, EKF, particle filter by assumptions.",
      "Recognize when each is appropriate."
    ],
    "prereqs": [
      "state-space-control"
    ]
  }
}
