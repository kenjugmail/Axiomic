[
  {
    "id": "q1",
    "question": "What does softmax produce from a vector of arbitrary real numbers?",
    "options": [
      "A probability distribution: positive values that sum to 1",
      "A vector of integers from 0 to 9",
      "The maximum value of the input",
      "A normalized vector with unit L2 norm"
    ],
    "correctIndex": 0
  },
  {
    "id": "q2",
    "question": "What is the purpose of the temperature parameter T in softmax(z / T)?",
    "options": [
      "T < 1 sharpens the distribution toward the argmax; T > 1 makes it more uniform",
      "T controls the learning rate during training",
      "T toggles between addition and multiplication",
      "T is only used for numerical stability"
    ],
    "correctIndex": 0
  },
  {
    "id": "q3",
    "question": "Why subtract the max before applying softmax in practice?",
    "options": [
      "Numerical stability — exponentiating large logits can overflow without affecting the result",
      "It reduces the L2 norm of the output",
      "It's required for the output to sum to 1",
      "It changes the distribution to be sharper"
    ],
    "correctIndex": 0
  },
  {
    "id": "q4",
    "kind": "slider",
    "question": "Drag the temperature T until the softmax distribution becomes approximately uniform across the five logits.",
    "viz": "softmax-temperature",
    "min": 0.1,
    "max": 10,
    "step": 0.1,
    "default": 1,
    "target": { "min": 4, "max": 10 },
    "explanation": "As T → ∞, softmax(z / T) flattens toward 1/N for every logit. By T ≈ 4-5 the bars are visually close to the dashed uniform line; higher still nudges them closer."
  },
  {
    "id": "q5",
    "kind": "code",
    "question": "Implement softmax. Given a 1-D numpy array of logits, return a 1-D array of probabilities. Don't forget numerical stability: subtract the max before exponentiating.",
    "functionName": "softmax",
    "starterCode": "def softmax(logits):\n    # logits is a numpy array. Return a numpy array of\n    # probabilities that sum to 1.\n    pass\n",
    "tests": [
      {
        "name": "Sums to 1 on a basic input",
        "inputs": ["np.array([2.0, 1.0, 0.5, -1.0])"],
        "check": "abs(float(result.sum()) - 1.0) < 1e-6"
      },
      {
        "name": "All outputs non-negative",
        "inputs": ["np.array([-3.0, 0.0, 5.0])"],
        "check": "bool(np.all(result >= 0))"
      },
      {
        "name": "Argmax is preserved",
        "inputs": ["np.array([0.1, 2.5, -0.3, 1.0])"],
        "check": "int(np.argmax(result)) == 1"
      },
      {
        "name": "Numerically stable on huge logits",
        "inputs": ["np.array([1000.0, 1001.0, 999.0])"],
        "check": "bool(np.isfinite(result).all()) and abs(float(result.sum()) - 1.0) < 1e-6"
      }
    ],
    "explanation": "The 'subtract the max' trick prevents overflow when logits are large. Without it, np.exp(1001) is inf and the sum becomes inf/inf = nan."
  }
]
