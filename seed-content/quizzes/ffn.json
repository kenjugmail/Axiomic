[
  {
    "id": "q1",
    "question": "What is the structure of the feed-forward block inside a transformer layer?",
    "options": [
      "Two linear projections with a non-linearity (e.g., GELU or SwiGLU) between them",
      "A single linear projection with no activation",
      "A recurrent neural network",
      "Just attention applied a second time"
    ],
    "correctIndex": 0
  },
  {
    "id": "q2",
    "question": "Why is the FFN typically widened (e.g., 4x d_model) in the middle?",
    "options": [
      "To give the layer more capacity to compute non-linear combinations of features before projecting back",
      "GPUs run faster on wider tensors regardless of computation",
      "Wider tensors use less memory",
      "It's needed for backpropagation to work"
    ],
    "correctIndex": 0
  },
  {
    "id": "q3",
    "question": "Is the FFN applied per-token or across the sequence?",
    "options": [
      "Per-token — the same FFN runs independently on each position",
      "It mixes information across all positions",
      "It only runs on the first token",
      "It's applied between layers, not within them"
    ],
    "correctIndex": 0
  },
  {
    "id": "q4",
    "kind": "code",
    "question": "Implement a 2-layer feed-forward network with ReLU. Given x of shape [n, d_in] and weight/bias matrices, return the output of shape [n, d_out].",
    "functionName": "ffn",
    "starterCode": "def ffn(x, W1, b1, W2, b2):\n    # x: [n, d_in]; W1: [d_in, d_hidden]; b1: [d_hidden]\n    # W2: [d_hidden, d_out]; b2: [d_out]\n    # Apply: linear -> ReLU -> linear. Return [n, d_out].\n    pass\n",
    "tests": [
      {
        "name": "Output shape matches d_out",
        "inputs": [
          "np.array([[1.0, 2.0]])",
          "np.array([[0.5, -0.5, 1.0], [-1.0, 1.5, 0.5]])",
          "np.zeros(3)",
          "np.array([[1.0], [-1.0], [0.5]])",
          "np.zeros(1)"
        ],
        "check": "result.shape == (1, 1)"
      },
      {
        "name": "ReLU clips negatives in the hidden layer",
        "inputs": [
          "np.array([[1.0]])",
          "np.array([[-2.0]])",
          "np.zeros(1)",
          "np.array([[3.0]])",
          "np.zeros(1)"
        ],
        "check": "bool(np.allclose(result, np.array([[0.0]])))"
      },
      {
        "name": "Identity-style fixture",
        "inputs": [
          "np.array([[1.0, 2.0]])",
          "np.eye(2)",
          "np.zeros(2)",
          "np.eye(2)",
          "np.zeros(2)"
        ],
        "check": "bool(np.allclose(result, np.array([[1.0, 2.0]])))"
      },
      {
        "name": "Bias is added correctly",
        "inputs": [
          "np.zeros((1, 2))",
          "np.eye(2)",
          "np.array([1.0, 1.0])",
          "np.eye(2)",
          "np.array([2.0, 3.0])"
        ],
        "check": "bool(np.allclose(result, np.array([[3.0, 4.0]])))"
      }
    ],
    "explanation": "x @ W1 + b1, then np.maximum(0, ...) for ReLU, then that @ W2 + b2. Numpy broadcasting handles the bias add per row. The 4x widening (d_hidden = 4 * d_in) in real transformers is what gives this layer its capacity."
  },
  {
    "id": "q5",
    "kind": "slider",
    "question": "Drag the input until Sigmoid saturates (i.e. its output is ≥ 0.9). Notice how GELU and Tanh behave very differently in the same range.",
    "viz": "activation-function-gallery",
    "min": -4,
    "max": 4,
    "step": 0.05,
    "default": 0,
    "target": { "min": 2.3, "max": 4 },
    "explanation": "Sigmoid(x) = 1/(1+e^-x) crosses 0.9 around x ≈ 2.2. Past that it asymptotes — gradients shrink — which is the historical reason ReLU/GELU replaced Sigmoid as a hidden-layer non-linearity in deep networks."
  }
]
